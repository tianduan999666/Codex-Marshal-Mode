# V4 当前真实状态总表

最后更新：2026-04-05
状态：现行标准件
适用范围：`V4-Trial / V4-Target / 本机运行态`
说明：本表只写当前已被证据确认的真实状态，不替未来计划代写完成度。

## 一句话结论

当前仓 `V4-Trial` 与 `V4-Target` 现有任务包都已收口；当前版本已统一到 `CX-202604051503`，本机运行态与真源一致，自检链为绿，面板传令冒烟为绿；当前没有激活任务。

## 当前真实状态总表

| 维度 | 当前真实状态 | 证据 |
| --- | --- | --- |
| Trial 完成度 | `35/35 done`；Trial 验收结论为“通过，但带缺陷进入下一阶段准备” | `.codex/chancellor/tasks/*/state.yaml`、`docs/40-执行/07-V4-Trial-验收报告.md` |
| Target 完成度 | `15/15 done`；现行推进顺序仍以 `T1 → T2 → T3 → T4 → T5` 为准 | `.codex/chancellor/tasks/*/state.yaml`、`docs/40-执行/12-V4-Target-实施计划.md` |
| 本机运行态同步情况 | 已同步 `34` 个受管文件；`verify_status=passed`；上次检查时间为 `2026-04-05 15:05:28`；轻检 `5/5` 哈希一致；`-MaintainerMode` 下也为绿 | `C:\Users\tianduan999\.codex\config\chancellor-mode\install-record.json`、`C:\Users\tianduan999\.codex\config\chancellor-mode\task-start-state.json` |
| 当前版本 | 源仓 / 运行态 / 安装记录统一为 `CX-202604051503` | `codex-home-export/VERSION.json`、`codex-home-export/manifest.json`、`C:\Users\tianduan999\.codex\config\cx-version.json`、`C:\Users\tianduan999\.codex\config\chancellor-mode\install-record.json` |
| 版本可信性 | `manifest.version = VERSION.json.cx_version = install-record.cx_version = runtime_version` | 同上 |
| 当前任务 | 无激活任务；`active-task.txt` 为空 | `.codex/chancellor/active-task.txt` |
| 本机可用性 | 是；`verify-panel-command-smoke.ps1` 已通过；`run-managed-self-check.ps1 -MaintainerMode` 已通过；最近一轮人工验板结果稿仍可回看 | `codex-home-export/verify-panel-command-smoke.ps1`、`codex-home-export/run-managed-self-check.ps1`、`logs/panel-acceptance-result-20260405-134803.md` |
| 整体预计完成时间 | 当前没有可信硬截止；现行文档只冻结推进顺序，不冻结总完工日期 | `docs/40-执行/12-V4-Target-实施计划.md` |
| 当前主要风险 | 仍有残余工程化风险，见下表 | `docs/40-执行/08-V4-Trial-缺陷清单.md`、本机安装记录 |

## 当前主要风险

| 风险 | 级别 | 当前现状 | 下一步 |
| --- | --- | --- | --- |
| `git index.lock` 偶发残留 | P1 | 已有现行处置方案，但尚未工程化根治 | 继续按 `docs/40-执行/10-本地安全提交流程.md` 治理，并在后续 Target 工程化中根治 |
| 任务包起包未完全自动化 | P2 | 已有半自动脚手架，但仍需人工提供任务输入与补细节 | 后续按 `T4` 决定是否继续增强 |
| 执行区证据稿仍需持续治理 | P2 | 已有归档规则与首轮迁移，但后续仍需持续执行 | 继续按 `docs/90-归档/01-执行区证据稿归档规则.md` 收口 |
| 本机仍检测到旧 `config/marshal-mode` 残留 | 低 | 本轮安装已识别残留，但默认不自动删除 | 待确认旧版本彻底废弃后，再由维护层清理 |
| 仓内模板 provider 与本机当前 provider 不同 | 低 | 当前模板为 `crs`，本机实际保留 `packycode`；本轮未覆盖用户配置 | 若要切模板配置，必须显式执行 `install.cmd -ApplyTemplateConfig` |

## 本轮已修平

1. 运行态与真源的 4 处漂移已修平，并通过安装同步回写到本机运行态。
2. 开工骨架已改为默认 `开场白 → 接令句`；只有真的进入检查阶段时才补固定边界提示。
3. 自检链已补 `MaintainerMode`，可一次性列出全部受管文件漂移。
4. 公开提交门禁已补“受管文件变更必须同步 `VERSION.json`”纪律，避免再出现同版本不同内容。
5. 当前状态总表已纳入现行标准件与导航入口，后续可直接以本表做快速复核。
6. 本轮已补面板命令说明、维护者模式入口、封顶边界清单与本地噪音处理规则，并同步 bump 到 `CX-202604051417` 后重新安装、自检。
7. 本轮已补“闭源项目统一代称默认写 `业务项目`，公开材料与提交信息禁止出现真实项目名”的脱敏规则，并同步 bump 到 `CX-202604051503` 后重新安装、验真。
