# V4 当前真实状态总表

最后更新：2026-04-06
状态：现行标准件
适用范围：`V4-Trial / V4-Target / 本机运行态`
说明：本表只写当前已被证据确认的真实状态，不替未来计划代写完成度。
维护动作：需要追平核心字段时，执行 `powershell.exe -ExecutionPolicy Bypass -File .\.codex\chancellor\sync-current-state-table.ps1`。

## 一句话结论

当前仓 `V4-Trial` 与 `V4-Target` 现有任务包都已收口；当前版本已统一到 `CX-202604060137`，本机运行态与真源一致，自检链为绿，面板传令冒烟为绿；`传令：交班 / 传令：接班` 与任务级进度快照已落地；丞相候选金句库已独立落盘到 `codex-home-export/quote-candidates.json`；一句话开工失败场景已补丞相式补信息提示；旧 `config/marshal-mode` 残留已转存到备份区；当前没有激活任务。

## 当前真实状态总表

| 维度 | 当前真实状态 | 证据 |
| --- | --- | --- |
| Trial 完成度 | `35/35 done`；Trial 验收结论为“通过，但带缺陷进入下一阶段准备” | `.codex/chancellor/tasks/*/state.yaml`、`docs/40-执行/07-V4-Trial-验收报告.md` |
| Target 完成度 | `15/15 done`；现行推进顺序仍以 `T1 → T2 → T3 → T4 → T5` 为准 | `.codex/chancellor/tasks/*/state.yaml`、`docs/40-执行/12-V4-Target-实施计划.md` |
| 本机运行态同步情况 | 已同步 `39` 个受管文件；`verify_status=passed`；上次检查时间为 `2026-04-06 02:04:54`；轻检 `5/5` 哈希一致 | `C:\Users\tianduan999\.codex\config\chancellor-mode\install-record.json`、`C:\Users\tianduan999\.codex\config\chancellor-mode\task-start-state.json` |
| 当前版本 | 源仓 / 运行态 / 安装记录统一为 `CX-202604060145` | `codex-home-export/VERSION.json`、`codex-home-export/manifest.json`、`C:\Users\tianduan999\.codex\config\cx-version.json`、`C:\Users\tianduan999\.codex\config\chancellor-mode\install-record.json` |
| 版本可信性 | `manifest.version = VERSION.json.cx_version = install-record.cx_version = runtime_version = CX-202604060145` | 同上 |
| 当前任务 | 无激活任务；`active-task.txt` 为空 | `.codex/chancellor/active-task.txt` |
| 本机可用性 | 是；`verify-panel-command-smoke.ps1` 已通过；`run-managed-self-check.ps1 -MaintainerMode` 已通过；`start-panel-acceptance.ps1` 已重新生成人工验板结果稿；`传令：交班 / 接班` 冒烟为绿 | `codex-home-export/verify-panel-command-smoke.ps1`、`codex-home-export/run-managed-self-check.ps1`、`codex-home-export/start-panel-acceptance.ps1`、`logs/panel-acceptance-result-20260405-165214.md` |
| 整体预计完成时间 | 当前没有可信硬截止；现行文档只冻结推进顺序，不冻结总完工日期 | `docs/40-执行/12-V4-Target-实施计划.md` |
| 当前主要风险 | 仍有残余工程化风险，见下表 | `docs/40-执行/08-V4-Trial-缺陷清单.md`、本机安装记录 |

## 当前主要风险

| 风险 | 级别 | 当前现状 | 下一步 |
| --- | --- | --- | --- |
| `git index.lock` 偶发残留 | P1 | 已有脚本化诊断/清理入口，且已收紧到“锁文件时间窗口内的 Git 进程”口径；误报已明显收敛，但仍缺更长周期实战观察 | 继续按 `docs/40-执行/10-本地安全提交流程.md` 治理，并在后续真实提交里继续观察是否还会高频中断 |
| 任务包起包未完全自动化 | P2 | 已有半自动脚手架，但仍需人工提供任务输入与补细节 | 后续按 `T4` 决定是否继续增强 |
| 执行区证据稿仍需持续治理 | P2 | 已有归档规则与首轮迁移，但后续仍需持续执行 | 继续按 `docs/90-归档/01-执行区证据稿归档规则.md` 收口 |
| 仓内模板 provider 与本机当前 provider 不同 | 低 | 当前模板为 `crs`，本机实际保留 `yunyi`；本轮未覆盖用户配置 | 若要切模板配置，必须显式执行 `install.cmd -ApplyTemplateConfig` |

## 本轮已修平

1. 运行态与真源的 4 处漂移已修平，并通过安装同步回写到本机运行态。
2. 开工骨架已改为默认 `开场白 → 接令句`；只有实际执行自检前才补固定边界提示。
3. 自检链已补 `MaintainerMode`，可一次性列出全部受管文件漂移。
4. 公开提交门禁已补“受管文件变更必须同步 `VERSION.json`”纪律，避免再出现同版本不同内容。
5. 当前状态总表已纳入现行标准件与导航入口，后续可直接以本表做快速复核。
6. 本轮已补面板命令说明、维护者模式入口、封顶边界清单与本地噪音处理规则，并同步 bump 到 `CX-202604051417` 后重新安装、自检。
7. 本轮已补“闭源项目统一代称默认写 `业务项目`，公开材料与提交信息禁止出现真实项目名”的脱敏规则，并同步 bump 到 `CX-202604051503` 后重新安装、验真。
8. 本轮已补 `传令：交班 / 传令：接班` 公开命令，当前激活任务目录下会自动读写 `progress-snapshot.md / handoff.md`。
9. 本轮已补任务级进度快照自动沉淀：一句话开工、继续当前任务、交班、接班都会刷新 `progress-snapshot.md`。
10. 本轮已冻结 6 个高频复用件，并写入 `AGENTS.md`、`codex-home-export/AGENTS.md` 与 `docs/40-执行/39-V4-高频复用件总表.md`。
11. 本轮已 bump 到 `CX-202604052120`，并完成重装、完整验真、维护者模式自检、交班/接班冒烟与人工验板结果稿重生成。
12. 本轮已 bump 到 `CX-202604052230`，并完成“边界提示只在实际执行自检前显示”的主链修正、重装、完整验真与公开仓推送。
13. 本轮已把本机旧 `config/marshal-mode` 整体转存到 `C:\Users\tianduan999\.codex\backup\legacy-marshal-mode-20260405-191227\config\marshal-mode`，当前安装入口已不再提示该残留。
14. 本轮已补 `sync-current-state-table.ps1` 与配套测试，可一键追平 Trial / Target 完成度、版本、运行态同步状态与当前任务到本表。
15. 本轮已把“推进汇报件”纳入双份 `AGENTS` 真源：连续多轮推进时，默认先给进度小表，再继续下一刀。
16. 本轮已 bump 到 `CX-202604052351`，并完成重装、面板传令冒烟、真实 provider/auth 鉴权与完整验真。
17. 本轮已把《丞相金句体系 2.0》附录候选句独立落盘为 `codex-home-export/quote-candidates.json`，并把“补范围提示”接到 `传令：继续 / 交班 / 接班` 的无激活任务场景；当前版本已 bump 到 `CX-202604060059` 并完成重装、面板冒烟与完整验真。
