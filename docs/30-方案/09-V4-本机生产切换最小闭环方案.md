# V4 本机生产切换最小闭环方案

最后更新：2026-03-29
状态：子线推进稿
适用范围：当前仓接管本机生产母体前的最小闭环设计

## 一句话结论

当前仓要想丝滑接管本机生产，不能直接整包搬旧母体；最小正确做法是先形成“生产母体职责 + 版本真源 + 切换/回滚 + 验板闭环”的四件套。

## 当前现状

1. 本机运行态版本镜像在 `~/.codex/config/cx-version.json`。
2. 当前镜像仍声明真源为 `codex-home-export`。
3. 旧参考仓仍持有现役 `codex-home-export/`。
4. 当前新仓已落地 `codex-home-export/`、`VERSION.json`、`install-to-home.ps1`、`rollback-from-backup.ps1` 与 `verify-cutover.ps1`，并具备自动验板闭环；仍建议保留面板人工验板。

## 本线最小目标

- 让当前仓具备“单机本机生产切换”能力。
- 切换后可通过版本镜像、安装脚本与固定验板步骤确认是否成功。
- 失败时可一键回滚，不碰用户凭据与隐私文件。

## 明确不做

- 不做第二机链路。
- 不做发布链或便携包链路。
- 不做整包照搬旧母体。
- 不在当前轮引入重型安装器重构。

## 最小闭环四件套

1. `codex-home-export/`：当前仓内的本机生产母体目录。
2. `VERSION.json`：母体版本真源。
3. `install-to-home.ps1` + 回滚脚本：负责单机切换与恢复。
4. 切换后验板清单：确认版本、入口命令与面板表现。

## 推荐推进顺序

### S1：合法化生产母体落点

- 先补根目录职责说明。
- 再补目录锁定清单与最小目录蓝图。
- 最后再新建 `codex-home-export/`。

### S2：落最小生产母体骨架

- 已落 `README.md`、`VERSION.json`、`manifest.json`、`install-to-home.ps1`、`initialize-workspace.ps1`、`new-task.ps1`、`rollback-from-backup.ps1` 与 `verify-cutover.ps1`。
- 现已具备自动安装、自动回滚、自动验板三件套。
- 2026-03-29 已在本机完成第一次真实桥接切换；是否最终通过，以面板人工验板为准。
- 2026-03-31 已重新执行 `codex-home-export/verify-cutover.ps1`，自动验板再次 `PASS`；当前最小闭环瓶颈已收敛为官方面板人工验板。
- 2026-03-31 已补 `start-panel-acceptance.ps1`、`panel-acceptance-three-step-card.md`、`panel-acceptance-pass-fail-sheet.md`、`panel-acceptance-result-template.md`、`new-panel-acceptance-result.ps1` 与 `verify-panel-acceptance-result.ps1`，把人工验板收敛为“一键准备 + 三步入口 + 打勾判断 + 结果留痕 + 一键起稿 + 结果复核 + 完整清单”七层结构。
- 当前已把 `VERSION.json`、`manifest.json`、`README.md` 的阶段/版本/文件清单一致性下沉到公开提交治理门禁。
- 当前已把 `AGENTS.md`、`VERSION.json.panel_commands` 与 `panel-acceptance-checklist.md` 的面板命令口径一致性下沉到公开提交治理门禁。
- 当前已把 `传令 版本`、`传令 检查`、`传令 状态` 的响应验收口径，同步下沉到 `docs/40-执行/03-面板入口验收.md` 与公开提交治理门禁。
- 当前已把 `传令 修复`、`传令 验板` 的动作边界，同步下沉到公开验收文档、人工验板清单与公开提交治理门禁。
- 当前已把 `传令 帮助` 的公开用法结构，同步下沉到入口验收文档、人工验板清单与公开提交治理门禁。
- 当前已把 `传令 帮助` 的“三段式固定结构”（当前用法 → 面板命令 → 注意事项）同步下沉到入口验收文档、人工验板清单与公开提交治理门禁。
- 当前已把 `传令 帮助` 的当前用法进一步细化为固定子项（默认入口 → 适用场景 → 维护层说明），同步下沉到入口验收文档、人工验板清单与公开提交治理门禁。
- 当前已把 `传令 帮助` 的面板命令说明进一步细化为固定子项（命令顺序 → 命令含义 → 公开边界），同步下沉到入口验收文档、人工验板清单与公开提交治理门禁。
- 当前已把 `传令 帮助` 的注意事项进一步细化为固定子项（安全边界 → 维护层动作 → 新开会话验板提醒），同步下沉到入口验收文档、人工验板清单与公开提交治理门禁。
- 当前已把 `传令 版本` 的输出进一步细化为固定槽位（版本号 → 版本来源 → 真源路径），同步下沉到入口验收文档、人工验板清单与公开提交治理门禁。
- 当前已把 `传令 检查` 的输出进一步细化为固定槽位（检查范围 → 检查结论 → 建议动作），同步下沉到入口验收文档、人工验板清单与公开提交治理门禁。
- 当前已把 `传令 状态` 的输出进一步细化为固定槽位（当前模式 → 稳态判断 → 下一步），同步下沉到入口验收文档、人工验板清单与公开提交治理门禁。
- 当前已把 `传令 修复` 的边界进一步细化为固定槽位（修复范围 → 处理方式 → 升级条件），同步下沉到入口验收文档、人工验板清单与公开提交治理门禁。
- 当前已把 `传令 验板` 的边界进一步细化为固定槽位（触发场景 → 验板动作 → 验板目标），同步下沉到入口验收文档、人工验板清单与公开提交治理门禁。
- 当前已把 `固定人工验收步骤 / 验板步骤` 进一步细化为固定子步骤模板（预处理动作 → 新开会话 → 首句验板 → 开头校验 → 语气校验 → 状态校验 → 任务一致性 / 关闭旧会话 → 新开官方面板 → 版本验证 → 检查验证 → 状态验证），同步下沉到入口验收文档、人工验板清单与公开提交治理门禁。
- 当前已把 `通过标准` 进一步细化为固定子项模板（模式稳定 → 帮助完整 → 命令口径 → 任务一致 → 复验闭环 / 命令有效 → 边界稳定 → 验板闭环 → 过程稳定 → 无需手改），同步下沉到入口验收文档、人工验板清单与公开提交治理门禁。
- 当前已把 `失败信号` 进一步细化为固定子项模板（模式失稳 → 帮助缺项 → 命令漂移 → 任务冲突 → 复验失败），同步下沉到入口验收文档与公开提交治理门禁。
- 当前已把 `失败后的处置动作 / 若不通过` 进一步细化为固定子项模板（停止扩展 → 回看变更 → 重新验板 → 缺陷收口 / 自动复核 → 自动回退 → 重新验板），同步下沉到入口验收文档、人工验板清单与公开提交治理门禁。
- 当前已把 `与试跑阶段的关系` 进一步细化为固定槽位（前置门槛 → 放行条件 → 公开边界），同步下沉到入口验收文档与公开提交治理门禁。
- 当前已把 `panel-acceptance-checklist.md` 的“通过标准”摘要列点固定为顺序清单（版本有效 → 检查有效 → 状态有效 → 维护边界稳定 → 验板步骤完整 → 过程稳定 → 无需手改），并同步下沉到公开提交治理门禁。
- 当前已把 `panel-acceptance-checklist.md` 的“若不通过”摘要列点固定为顺序清单（自动复核 → 自动回退 → 重新验板），并同步下沉到公开提交治理门禁。
- 当前已把 `panel-acceptance-checklist.md` 的“验板步骤”摘要列点固定为顺序清单（关闭旧会话 → 新开官方面板 → 版本验证 → 检查验证 → 状态验证），并同步下沉到公开提交治理门禁。
- 当前已把 `panel-acceptance-checklist.md` 的头部引导说明固定为顺序双行（适用场景 → 验板命令口径真源），并同步下沉到公开提交治理门禁。
- 当前已把 `03-面板入口验收.md` 的“失败后的处置动作”摘要列点固定为顺序清单（停止扩展 → 回看变更 → 重新验板 → 缺陷收口），并同步下沉到公开提交治理门禁。
- 当前已把 `03-面板入口验收.md` 的“失败信号”摘要列点固定为顺序清单（首句失稳 → 固定开头丢失 → 帮助结构缺项 → 人工验收步骤缺项 → 版本/检查/状态口径缺项 → 维护边界缺项 → 任务冲突），并同步下沉到公开提交治理门禁。
- 当前已把 `03-面板入口验收.md` 的“通过标准”摘要列点固定为顺序清单（首句进入丞相模式 → 固定开头稳定 → 帮助结构完整 → 人工验收步骤完整 → 版本/检查/状态口径完整 → 维护边界完整 → 任务一致 → 复验闭环），并同步下沉到公开提交治理门禁。
- 当前已把 `03-面板入口验收.md` 的“一句话目标”固定为唯一公开目标句（入口语义稳定 → 任务包衔接明确 → 固定人工验收闭环），并同步下沉到公开提交治理门禁。
- 当前已把 `03-面板入口验收.md` 的“本轮不验收”摘要列点固定为顺序清单（真实业务任务是否跑通 → 安装链与发布链是否稳定 → 多 Agent、长期记忆、独立 UI），并同步下沉到公开提交治理门禁。
- 当前已把 `03-面板入口验收.md` 的“验收范围”摘要列点固定为顺序清单（首句入口风格 → 最小响应口径 → 任务包衔接 → 人工复验步骤），并同步下沉到公开提交治理门禁。
- 当前已把 `03-面板入口验收.md` 的“验收范围”细化为固定槽位（首句入口风格 → 最小响应口径 → 任务包衔接 → 人工复验步骤），并同步下沉到公开提交治理门禁。
- 当前已把 `03-面板入口验收.md` 接入维护层总入口的补充能力真源，并同步纳入 README / docs README / 公开提交治理门禁的一致性校验。
- 当前已把 `13-维护层总入口.md` 的“推荐使用顺序”摘要列点固定为顺序清单（动作判类 → 进入单项规则 → 追加治理复核 → 配置来源落盘 → 确认 pre-push 门禁 → 收口检查 → 回看入口同步），并同步下沉到公开提交治理门禁。
- 当前已把 `13-维护层总入口.md` 的“当前默认原则”摘要列点固定为顺序清单（维护层边界 → 复用已有规则与脚本 → 入口口径同步），并同步下沉到公开提交治理门禁。
- 当前已把 `13-维护层总入口.md` 的“什么时候优先看这份入口”摘要列点固定为顺序清单（入口不明确 → 动作开始前 → 交接前），并同步下沉到公开提交治理门禁。
- 当前已把 `13-维护层总入口.md` 的“本文档的价值”摘要列点固定为顺序清单（单一入口 → 缩短上手路径 → 降低误操作概率），并同步下沉到公开提交治理门禁。
- 当前已把 `docs/40-执行/README.md` 的底部真源说明固定为顺序四行（现行真源 → 时间戳稿边界 → 区分说明入口 → 归档规则入口），并同步下沉到公开提交治理门禁。
- 当前已把 `docs/40-执行/README.md` 的顶部用途摘要固定为顺序四项（试运行计划 → 任务清单 → 执行记录 → 验收单），并同步下沉到公开提交治理门禁。
- 当前已把 `docs/40-执行/README.md` 的标题固定为唯一入口标题（`# 40-执行 目录说明`），并同步下沉到公开提交治理门禁。
- 当前已把 `04-执行区现行件与证据稿说明.md` 的“一句话结论”固定为唯一公开结论句（固定编号文件是现行标准件 → 时间戳文件默认是证据稿或过程稿），并同步下沉到公开提交治理门禁。
- 当前已把 `04-执行区现行件与证据稿说明.md` 的“使用顺序”固定为顺序清单（先读任务包规范 → 再读任务包模板 → 试跑前读面板入口验收 → 追溯历史时再看时间戳证据稿），并同步下沉到公开提交治理门禁。
- 当前已把 `04-执行区现行件与证据稿说明.md` 的“证据稿与过程稿”固定为顺序清单（带时间戳执行文档 → 提炼/冻结/过程说明稿 → 仅用于还原判断过程的阶段性文档），并同步下沉到公开提交治理门禁。
- 当前已把 `04-执行区现行件与证据稿说明.md` 的“命名与维护规则”固定为顺序清单（现行标准件优先固定编号命名 → 时间戳稿不得默认视为现行件 → 差异时以固定编号文件为准 → 失效时间戳稿转入归档区），并同步下沉到公开提交治理门禁。
- 当前已把 `04-执行区现行件与证据稿说明.md` 的“已迁入归档区的证据稿”固定为顺序清单（MVP 边界与首任务包 → Trial-001 边界冻结），并同步下沉到公开提交治理门禁。
- 当前已把 `04-执行区现行件与证据稿说明.md` 的“本文件的价值”固定为顺序清单（降低检索成本 → 避免误把过程稿当现行件 → 保持现行件少而稳、证据稿可追溯），并同步下沉到公开提交治理门禁。
- 当前已把 `14-维护层动作矩阵与收口检查表.md` 的“一句话结论”固定为唯一公开结论句（先用动作矩阵判断路径 → 再按收口检查表完成留痕、导航同步、提交与推送），并同步下沉到公开提交治理门禁。
- 当前已把 `14-维护层动作矩阵与收口检查表.md` 的“维护入口同步”细化为固定槽位（适用边界 → 主入口真源 → 同步范围 → 收口要求），并同步下沉到公开提交治理门禁。
- 当前已把 `14-维护层动作矩阵与收口检查表.md` 的“基础收口”细化为固定槽位（动作归类 → 文档沉淀 → 本地留痕 → 导航同步 → 提交串行 → 门禁通过 → 下一步说明），并同步下沉到公开提交治理门禁。
- 当前已把 `14-维护层动作矩阵与收口检查表.md` 的“治理审计补充检查”细化为固定槽位（来源依据 → 决策留痕 → 输出追溯 → 入口修平 → 边界复核），并同步下沉到公开提交治理门禁。
- 当前已把 `14-维护层动作矩阵与收口检查表.md` 的“默认决策顺序”细化为固定槽位（动作判类 → 进入主入口 → 追加治理复核 → 执行统一收口 → 结束条件），并同步下沉到公开提交治理门禁。
- 当前已把 `14-维护层动作矩阵与收口检查表.md` 的“公开仓边界提醒”细化为固定槽位（公开范围 → 本地运行态 → 依赖约束），并同步下沉到公开提交治理门禁。
- 当前已把 `14-维护层动作矩阵与收口检查表.md` 的“推荐搭配关系”细化为固定槽位（提交动作 → 起包动作 → 归档动作 → 入口变更 → 拍板包准备 → 拍板包半自动起手 → 拍板结果回写 → 异常路径或回退记录 → 多 gate / 多异常裁决 → 复杂并存汇报落盘 → 关键配置来源与漂移检查落盘 → 公开口径变更前），并同步下沉到公开提交治理门禁。
- 当前已把 `14-维护层动作矩阵与收口检查表.md` 的“维护层动作矩阵”固定为顺序表格 / 行级矩阵（动作类型 → 什么时候用 → 主入口 → 最低产出 → 公开仓边界 → 风险级别），并同步下沉到公开提交治理门禁。
- 当前已把 `14-维护层动作矩阵与收口检查表.md` 的“本文档的价值”细化为固定槽位（固定套路 → 降低经验依赖 → 保留控制平面），并同步下沉到公开提交治理门禁。
- 当前已把 `15-拍板包准备与收口规范.md` 的“一句话结论 / 必须准备条件 / 最低组成”细化为固定槽位（先形成标准拍板包 → `must_gate` / `gates.yaml` / `waiting_gate` / 边界风险取舍 → 结论 / 选项 / 影响 / 建议），并同步下沉到公开提交治理门禁。
- 当前已把 `16-拍板包半自动模板.md` 的“一句话结论 / 适用场景 / 输出结果”细化为固定槽位（优先使用模板而非手工分别改四个文件 → 标准任务包 5 件套 / 首个待拍板事项 / `gates.yaml` 为空 / 切到 `waiting_gate` → `gates.yaml` / `state.yaml` / `decision-log.md` / `result.md`），并同步下沉到公开提交治理门禁。
- 当前已把 `17-拍板结果回写模板.md` 的“一句话结论 / 适用场景 / 输出结果”细化为固定槽位（优先使用回写模板而非手工分别改四个文件 → `pending` gate / 主公明确结论 / 从 `waiting_gate` 恢复真实状态 / 结果沉淀进任务包 → `gates.yaml` / `state.yaml` / `decision-log.md` / `result.md`），并同步下沉到公开提交治理门禁。
- 当前已把 `18-异常路径与回退模板.md` 的“一句话结论 / 适用场景 / 输出结果”细化为固定槽位（优先使用异常模板而非只在聊天里说“先停一下” → 动作失败 / 需要回退 / 等待外部协助 / 写入异常原因与恢复提示 → `state.yaml` / `decision-log.md` / `result.md`），并同步下沉到公开提交治理门禁。
- 当前已把 `21-关键配置来源与漂移复核模板.md` 的“推荐复核来源”细化为固定槽位（公开入口 → 文档总览入口 → 现行件导航 → 推进顺序依据 → 冻结边界依据），并同步下沉到公开提交治理门禁。
- 当前已把 `21-关键配置来源与漂移复核模板.md` 的“最低产出”细化为固定槽位（结果落盘 → 决策留痕 → 先修平再提交），并同步下沉到公开提交治理门禁。
- 当前已把 `21-关键配置来源与漂移复核模板.md` 的“推荐脚本入口”细化为固定槽位（脚本入口 → 治理依据 → 收口入口），并同步下沉到公开提交治理门禁。
- 当前已把 `21-关键配置来源与漂移复核模板.md` 的“一句话结论”细化为固定槽位（先回看来源 → 统一落盘复核），并同步下沉到公开提交治理门禁。
- 当前已把 `21-关键配置来源与漂移复核模板.md` 的“什么时候用”细化为固定槽位（现行标准件变更 → 公开改动提交前 → 解释现行依据 → 怀疑存在漂移 → 提交前预检落盘），并同步下沉到公开提交治理门禁。
- 当前已把 `21-关键配置来源与漂移复核模板.md` 的“result.md 推荐骨架”细化为固定槽位（复核模板 → 治理规范 → 配置来源 → 版本与现行依据 → 漂移检查 → 复核结论 → 下一步），并同步下沉到公开提交治理门禁。
- 当前已把 `21-关键配置来源与漂移复核模板.md` 的“decision-log.md 推荐骨架”细化为固定槽位（决策 → 证据 → 结论 → 影响），并同步下沉到公开提交治理门禁。
- 当前已把 `21-关键配置来源与漂移复核模板.md` 的“长期价值”细化为固定槽位（可重复落盘 → 提前暴露漂移 → 目录内自含），并同步下沉到公开提交治理门禁。
- 当前已把 `20-复杂并存汇报骨架模板.md` 的“维护层价值”细化为固定槽位（固定骨架 → 口径稳定 → 最小治理复核 → 自动化入口），并同步下沉到公开提交治理门禁。
- 当前已把 `20-复杂并存汇报骨架模板.md` 的“最低产出”细化为固定槽位（主状态落盘 → 次要事项与恢复顺序 → 裁决留痕 → 治理复核 → 状态同步），并同步下沉到公开提交治理门禁。
- 当前已把 `20-复杂并存汇报骨架模板.md` 的“推荐脚本入口”细化为固定槽位（脚本 → 规则 → 治理 → 收口），并同步下沉到公开提交治理门禁。
- 当前已把 `20-复杂并存汇报骨架模板.md` 的“半自动写入建议”细化为固定槽位（TaskId → PrimaryStatus → PrimaryBlocker → PrimaryReason → SecondaryItems → RecoverySteps → NextAction → DecisionBasis → RejectedCandidates → SyncState），并同步下沉到公开提交治理门禁。
- 当前已把 `20-复杂并存汇报骨架模板.md` 的``result.md` 推荐骨架`细化为固定槽位（主状态 → 主阻塞 → 主阻塞原因 → 主规则 → 骨架模板 → 治理复核入口 → 次要待处理项 → 恢复顺序 → 下一步 → 治理复核结果），并同步下沉到公开提交治理门禁。
- 当前已把 `20-复杂并存汇报骨架模板.md` 的``decision-log.md` 推荐骨架`细化为固定槽位（决策 → 主阻塞 → 原因 → 证据 → 未选状态 → 裁决依据 → 治理提示 → 影响），并同步下沉到公开提交治理门禁。
- 当前已把 `20-复杂并存汇报骨架模板.md` 的“一句话结论”细化为固定槽位（先选主状态 → 一次性落盘），并同步下沉到公开提交治理门禁。
- 当前已把 `20-复杂并存汇报骨架模板.md` 的“什么时候用”细化为固定槽位（已选主状态 → 同步复杂裁决 → 超出单一模板），并同步下沉到公开提交治理门禁。
- 当前已把 `19-多 gate 与多异常并存处理规则.md` 的“文档落盘分工”细化为固定槽位（state.yaml → gates.yaml → decision-log.md → result.md），并同步下沉到公开提交治理门禁。
- 当前已把 `19-多 gate 与多异常并存处理规则.md` 的“收口检查”细化为固定槽位（主状态排他性 → 次要事项保留 → result.md 完整性 → decision-log.md 留痕 → 恢复后重评），并同步下沉到公开提交治理门禁。
- 当前已把 `19-多 gate 与多异常并存处理规则.md` 的“推荐汇报顺序”细化为固定槽位（主状态结论 → 主阻塞理由 → 次要待处理项 → 恢复顺序），并同步下沉到公开提交治理门禁。
- 当前已把 `19-多 gate 与多异常并存处理规则.md` 的“单任务单主状态”细化为固定槽位（state.yaml 单主状态 → 次要事项留档 → 主阻塞唯一），并同步下沉到公开提交治理门禁。
- 当前已把 `19-多 gate 与多异常并存处理规则.md` 的“按下一行动主体决定优先级”细化为固定槽位（waiting_assist → waiting_gate → paused → ready_to_resume → running / ready / verifying / done），并同步下沉到公开提交治理门禁。
- 当前已把 `19-多 gate 与多异常并存处理规则.md` 的“多个 gate 并存时的主次划分”细化为固定槽位（主 gate 唯一 → 直接阻断优先 → 影响范围次级 → 截止时间兜底 → 次要待处理项留档），并同步下沉到公开提交治理门禁。
- 当前已把 `19-多 gate 与多异常并存处理规则.md` 的“gate 与异常并存时的裁决”细化为固定槽位（讨论条件未满足 → 异常收口后待拍板 → 已拍板待恢复 → 拍板回退先回写），并同步下沉到公开提交治理门禁。
- 默认保留“旧母体作参考底座”的过渡能力。

### S3：补验板闭环

- 校验 `~/.codex/config/cx-version.json`
- 校验必要脚本/配置是否到位
- 新开面板会话执行固定验板

## 验收口径

- 不覆盖用户凭据文件。
- 不要求人工记忆切换顺序。
- 切换失败可回滚。
- 切换后版本来源、切换来源与验板结果可追溯。
