# 丞相模式效果补足优化方案

最后更新：2026-04-03
状态：现行方案设计
目标：基于实际瓶颈的务实优化方案
参考来源：ECC、Claw-code、OpenClaw 精华提炼

## 一、现状分析与理论极限

### 1.1 当前实测瓶颈（来自 V4-Trial 缺陷清单）

**P1 级瓶颈**：
1. `git index.lock` 偶发残留 — 需串行清锁，降低连续推进稳定性
2. 任务包起包半自动化 — 仍需人工提供输入并补齐细节
3. 证据稿治理手动化 — 需持续人工归档

**实测时间消耗**（单个中等任务）：
- 任务包起包：5-8 分钟（手动填写 contract.yaml、创建目录结构）
- 执行过程：20-40 分钟（AI 编码 + 人工审查）
- 提交收口：3-5 分钟（检查门禁、手动提交、处理冲突）
- 证据归档：2-3 分钟（识别过程稿、移动到归档区）

**总计**：30-56 分钟/任务，平均 43 分钟

### 1.2 理论优化极限分析

**任务包起包**：
- 当前：5-8 分钟手动填写
- 理论极限：30 秒（模板自动生成 + 用户确认）
- 优化空间：**10-16 倍**（但实际占比仅 12%）

**执行过程**：
- 当前：20-40 分钟（单 AI 串行）
- 理论极限：10-20 分钟（Agent 并行 + 自动审查）
- 优化空间：**2 倍**（占比 70%，是主要瓶颈）

**提交收口**：
- 当前：3-5 分钟（手动检查 + 处理冲突）
- 理论极限：30 秒（自动门禁 + 自动清锁）
- 优化空间：**6-10 倍**（但实际占比仅 9%）

**证据归档**：
- 当前：2-3 分钟手动归档
- 理论极限：10 秒自动归档
- 优化空间：**12-18 倍**（但实际占比仅 5%）

**理论极限计算**（基于阿姆达尔定律）：
- 起包优化后：8 分钟 → 0.5 分钟
- 执行优化后：30 分钟 → 15 分钟
- 收口优化后：4 分钟 → 0.5 分钟
- 归档优化后：3 分钟 → 0.17 分钟
- **优化后总耗时**：0.5 + 15 + 0.5 + 0.17 = **16.17 分钟**
- **理论总提升**：43 / 16.17 = **2.66 倍**

**务实目标**：考虑实施成本和技术风险，**目标 2.5-2.7 倍提升**

### 1.3 三者精华提炼

**丞相模式核心优势**：
- 反屎山总纲：100% 合规的质量铁律
- 任务包持久化：跨轮恢复、完整证据链
- 文档治理：现行标准件体系

**ECC 核心优势**：
- 36 个专业 Agent 分工协作
- Instincts 持续学习系统
- 标准化工作流自动化

**Claw-code 核心优势**：
- 84 个 Hooks 编排层：判断型 + 自动化分离
- 分发器模式：单一入口、顺序执行、共享状态
- 规划-执行分离：避免上下文窗口竞争

**OpenClaw 核心优势**：
- 事件驱动 RPC 模型：实时工具拦截
- 分层策略优先级：Tool → Provider → Global → Agent → Sandbox
- 会话隔离边界：main/DM/group 不同权限

## 二、优化方案设计

### 2.1 Phase 1：自动化基础设施（4 周，目标 1.8 倍提升）

**核心目标**：消除重复性手工操作

**1. 任务包自动起包（节省 5-7 分钟）**
```powershell
# .codex/chancellor/auto-create-task.ps1
# 输入：用户一句话需求
# 输出：完整任务包骨架（contract.yaml + 目录结构）
# 实现：模板引擎 + 智能填充
```

**2. Git 冲突自动处理（节省 2-4 分钟）**
```powershell
# .codex/chancellor/auto-commit-safe.ps1
# 功能：
# - 自动检测并清理 index.lock
# - 自动重试提交（最多 3 次）
# - 失败时自动降级到手动模式
```

**3. 证据稿自动归档（节省 2-3 分钟）**
```powershell
# .codex/chancellor/auto-archive-evidence.ps1
# 规则：
# - 检测带时间戳的 .md 文件
# - 自动移动到 docs/90-归档/
# - 更新索引文件
```

**预期效果**：
- 起包时间：8 分钟 → 1 分钟（8 倍）
- 收口时间：4 分钟 → 30 秒（8 倍）
- 归档时间：3 分钟 → 10 秒（18 倍）
- **总体提升**：43 分钟 → 33 分钟（**1.3 倍**）

### 2.2 Phase 2：Agent 并行编排（6 周，目标 1.6 倍提升）

**核心目标**：执行阶段并行化

**1. 核心 Agent 集成**
- `planner` — 任务规划
- `tdd-guide` — 测试驱动开发
- `code-reviewer` — 代码审查
- `security-reviewer` — 安全审查
- `build-error-resolver` — 构建错误修复

**2. 并行执行策略**
```yaml
# 示例：并行审查
parallel_agents:
  - agent: code-reviewer
    target: src/**/*.ts
  - agent: security-reviewer
    target: src/auth/**/*.ts
  - agent: typescript-reviewer
    target: src/**/*.ts
```

**3. 结果聚合机制**
- 自动收集各 Agent 输出
- 冲突检测与优先级裁决
- 统一写入 decision-log.md

**预期效果**：
- 执行时间：30 分钟 → 20 分钟（1.5 倍）
- 审查质量：提升 30%（减少遗漏）
- **总体提升**：33 分钟 → 23 分钟（**1.4 倍**，累计 1.87 倍）

### 2.3 Phase 3：持续学习机制（8 周，目标 1.4 倍提升）

**核心目标**：减少重复错误

**1. Instincts 模式提取**
```yaml
# .codex/chancellor/instincts/patterns.yaml
patterns:
  - id: git-lock-retry
    trigger: "index.lock exists"
    action: "wait 1s and retry"
    confidence: 0.95
    success_count: 23
    
  - id: typescript-import-fix
    trigger: "Cannot find module"
    action: "check tsconfig paths"
    confidence: 0.88
    success_count: 15
```

**2. 自动错误纠正**
- 识别重复失败模式
- 自动应用已验证的修复方案
- 记录成功率并调整置信度

**3. 团队规范共享**
- 提取项目特定规范
- 自动注入到后续任务
- 跨任务包知识传递

**预期效果**：
- 重复错误减少 60%
- 调试时间减少 40%
- **总体提升**：23 分钟 → 18 分钟（**1.28 倍**，累计 2.39 倍）

### 2.4 Phase 4：质量门禁增强（4 周，目标 1.2 倍提升）

**核心目标**：提前发现问题

**1. 反屎山总纲自动检查**
```powershell
# .codex/chancellor/anti-shitcode-check.ps1
# 6 项强制自检：
# 1. 目录锁定合规
# 2. 现行标准件同步
# 3. 任务包完整性
# 4. 证据链完整性
# 5. 提交信息规范
# 6. 文档入口一致性
```

**2. 多层门禁体系**
- Pre-commit：本地快速检查
- Pre-push：完整合规检查
- Post-merge：跨任务包一致性检查

**3. 自动修复建议**
- 检测到问题时提供修复脚本
- 一键应用常见修复
- 降低人工干预成本

**预期效果**：
- 提交失败率降低 70%
- 返工时间减少 50%
- **总体提升**：18 分钟 → 16 分钟（**1.13 倍**，累计 2.69 倍）

## 三、实施路线图

### 3.1 总体时间表

```
Week 1-4:  Phase 1 - 自动化基础设施
Week 5-10: Phase 2 - Agent 并行编排
Week 11-18: Phase 3 - 持续学习机制
Week 19-22: Phase 4 - 质量门禁增强
```

### 3.2 里程碑与验收标准

**Phase 1 验收**：
- ✅ 任务包起包时间 < 1 分钟
- ✅ Git 冲突自动处理成功率 > 90%
- ✅ 证据稿自动归档覆盖率 100%

**Phase 2 验收**：
- ✅ 至少 3 个 Agent 并行工作
- ✅ 执行时间减少 30%
- ✅ 审查覆盖率提升 25%

**Phase 3 验收**：
- ✅ 识别并记录 20+ 个有效模式
- ✅ 重复错误减少 50%
- ✅ 自动修复成功率 > 70%

**Phase 4 验收**：
- ✅ 提交失败率 < 5%
- ✅ 门禁检查时间 < 10 秒
- ✅ 自动修复覆盖率 > 60%

### 3.3 风险与降级策略

**技术风险**：
1. Agent 并行冲突 → 降级到串行模式
2. Instincts 误判 → 人工确认机制
3. 门禁误报 → 白名单豁免机制

**系统级核心风险**（新增）：
1. **上下文窗口爆炸**（Token 脂肪肝）
   - 风险：并行 Agent、质量门禁、Instincts 日志疯狂填充 Payload
   - 应对：Phase 3 必须实现状态文件裁剪（State Truncation），丢弃非活跃上下文

2. **并发触发限流阻击**（Rate Limits）
   - 风险：5 个并发请求触发 RPM/TPD 限流导致大面积报错
   - 应对：dispatcher 必须实现指数退避（Exponential backoff）与排队机制

3. **门禁雪崩式死锁**
   - 风险：大模型幻觉导致永远不合规，自动化流程变成牢房
   - 应对：必须存在硬性 Bypass 机制，主公拥有强行越过门禁直接提交的权限

**成本风险**：
1. 开发时间超预期 → 分阶段交付
2. 维护成本高 → 自动化测试覆盖
3. 学习曲线陡峭 → 渐进式迁移

**降级路径**：
- 所有新能力均可独立禁用
- 保持现有手动流程可用
- 提供一键回退到 V4 模式
- **紧急豁免**：主公可通过 `--force-bypass` 强行越过所有门禁

## 四、核心技术实现

### 4.1 自动起包脚本示例

```powershell
# .codex/chancellor/auto-create-task.ps1
param(
    [string]$UserInput = ""
)

# 1. 解析用户输入
$taskName = $UserInput -replace '[^a-zA-Z0-9\u4e00-\u9fa5]', '-'
$timestamp = Get-Date -Format "yyyyMMdd-HHmmss"
$taskId = "$timestamp-$taskName"

# 2. 创建任务包目录
$taskDir = "tasks/$taskId"
New-Item -ItemType Directory -Path $taskDir -Force

# 3. 生成 contract.yaml
@"
task_id: $taskId
title: $UserInput
created_at: $(Get-Date -Format "yyyy-MM-dd HH:mm:ss")
status: planning
priority: normal
estimated_hours: 2
"@ | Out-File "$taskDir/contract.yaml" -Encoding UTF8

# 4. 生成其他文件
New-Item -ItemType File -Path "$taskDir/state.yaml" -Force
New-Item -ItemType File -Path "$taskDir/decision-log.md" -Force
New-Item -ItemType File -Path "$taskDir/result.md" -Force
New-Item -ItemType File -Path "$taskDir/gates.yaml" -Force

Write-Host "✅ 任务包已创建：$taskDir"
```

### 4.2 Git 安全提交脚本示例

```powershell
# .codex/chancellor/auto-commit-safe.ps1
param(
    [string]$Message = ""
)

$maxRetries = 3
$retryCount = 0
$backoffSeconds = 1

while ($retryCount -lt $maxRetries) {
    # 检查并清理 index.lock
    $lockFile = ".git/index.lock"
    if (Test-Path $lockFile) {
        Write-Host "⚠️ 检测到 index.lock，正在清理..."
        Remove-Item $lockFile -Force
        Start-Sleep -Seconds $backoffSeconds
    }
    
    # 尝试提交
    try {
        git add -A
        git commit -m $Message
        Write-Host "✅ 提交成功"
        exit 0
    }
    catch {
        $retryCount++
        Write-Host "❌ 提交失败，重试 $retryCount/$maxRetries"
        # 指数退避
        Start-Sleep -Seconds $backoffSeconds
        $backoffSeconds = $backoffSeconds * 2
    }
}

Write-Host "❌ 提交失败，请手动处理"
exit 1
```

**跨平台兼容性说明**：
- 当前脚本为 PowerShell 实现（Windows 优先）
- 如需跨平台支持，建议统一收敛到 Node.js 或 Python 实现
- 避免混用 .ps1 和 .sh，防止环境摩擦

### 4.3 Agent 并行编排示例

```yaml
# .codex/chancellor/agent-workflow.yaml
workflow:
  name: code-review-parallel
  
  stages:
    - name: parallel-review
      type: parallel
      agents:
        - name: code-reviewer
          input: "Review all TypeScript files"
          output: code-review-result.md
          
        - name: security-reviewer
          input: "Check for security vulnerabilities"
          output: security-review-result.md
          
        - name: typescript-reviewer
          input: "Check TypeScript type safety"
          output: type-review-result.md
    
    - name: aggregate-results
      type: sequential
      script: |
        # 使用文件锁防止并发写入撕裂
        (
          flock -x 200
          cat code-review-result.md >> decision-log.md
          echo -e "\n---\n" >> decision-log.md
          cat security-review-result.md >> decision-log.md
          echo -e "\n---\n" >> decision-log.md
          cat type-review-result.md >> decision-log.md
        ) 200>/tmp/decision-log.lock
```

**并发安全说明**：
- 使用 `flock` 文件锁防止多进程写入撕裂
- Windows 环境需使用等效的互斥锁机制
- 建议使用队列化写入而非直接 `cat >>` 追加

**并发限流应对**：
- dispatcher 必须实现 Rate Limit 感知
- 检测到 429 错误时自动指数退避
- 维护请求队列，避免同时发起过多并发请求

### 4.4 Instincts 模式提取示例

```yaml
# .codex/chancellor/instincts/patterns.yaml
version: 1.0
last_updated: 2026-04-03

patterns:
  - id: git-lock-auto-retry
    category: git-conflict
    trigger:
      type: error
      pattern: "index.lock"
    action:
      type: knowledge
      description: "Git index.lock 文件残留通常由进程异常退出导致，安全删除后重试即可"
      prompt_injection: "如遇到 index.lock 错误，先检查是否有 git 进程残留，然后删除 .git/index.lock 并重试提交"
    confidence: 0.95
    success_count: 23
    failure_count: 1
    last_success: 2026-04-02
    
  - id: typescript-import-path-fix
    category: build-error
    trigger:
      type: error
      pattern: "Cannot find module.*from"
    action:
      type: knowledge
      description: "TypeScript 模块解析失败通常是 tsconfig.json 的 paths 配置问题"
      prompt_injection: "检查 tsconfig.json 中的 paths 和 baseUrl 配置，确保模块路径映射正确"
    confidence: 0.88
    success_count: 15
    failure_count: 2
    last_success: 2026-04-01
    
  - id: missing-dependency-auto-install
    category: build-error
    trigger:
      type: error
      pattern: "Cannot find package"
    action:
      type: knowledge
      description: "缺少依赖包时应先检查 package.json，然后运行 npm install"
      prompt_injection: "运行 npm install 安装缺失的依赖包"
    confidence: 0.92
    success_count: 31
    failure_count: 3
    last_success: 2026-04-03
```

**Instincts 实施策略**（修正）：
- **Phase 3 前期**：仅收集"防坑知识"，作为系统 Prompt 注入，交给大模型理解
- **Phase 3 后期**：在积累足够样本后，才考虑自动化脚本拦截
- **避免过早自动化**：不要前期就上强行的逻辑脚本拦截，防止误杀和死板
- **人工审核机制**：所有自动应用的修复方案必须经过人工确认

## 五、成本收益分析

### 5.1 开发成本估算

| Phase | 工作量（人周） | 主要工作 |
|-------|--------------|---------|
| Phase 1 | 4 周 | 自动化脚本开发、测试 |
| Phase 2 | 6 周 | Agent 集成、并行调度 |
| Phase 3 | 8 周 | Instincts 引擎、模式库 |
| Phase 4 | 4 周 | 门禁增强、自动修复 |
| **总计** | **22 周** | **约 5.5 个月** |

### 5.2 收益量化

**单任务时间节省**：
- 优化前：43 分钟/任务
- 优化后：16 分钟/任务
- 节省：27 分钟/任务（**2.69 倍提升**）

**月度收益**（假设 20 个任务/月）：
- 节省时间：27 分钟 × 20 = 540 分钟 = **9 小时/月**
- 年度节省：9 × 12 = **108 小时/年**

**质量收益**：
- 提交失败率：15% → 5%（降低 67%）
- 重复错误：30% → 12%（降低 60%）
- 审查覆盖率：70% → 88%（提升 25%）

### 5.3 投资回报周期

- 开发成本：22 周 × 40 小时 = 880 小时
- 月度节省：9 小时
- **回本周期**：880 ÷ 9 ≈ **98 个月**（8.2 年）

**注意**：这是单人使用的回本周期。如果：
- 团队规模 5 人：回本周期 ≈ 20 个月
- 团队规模 10 人：回本周期 ≈ 10 个月

## 六、总结与建议

### 6.1 核心结论

1. **理论极限**：基于实测瓶颈分析，理论最大优化空间为 **3.43 倍**
2. **务实目标**：考虑实施成本和技术风险，目标 **2.5-3 倍提升**
3. **实际达成**：通过 4 个 Phase 的渐进式优化，预期达成 **2.69 倍提升**
4. **投资回报**：单人使用回本周期较长（8 年），但团队使用（5+ 人）回本周期可缩短至 1-2 年

### 6.2 优先级建议

**立即启动**（ROI 最高）：
1. Phase 1 - 自动化基础设施（4 周，1.3 倍提升）
   - 任务包自动起包
   - Git 冲突自动处理
   - 证据稿自动归档

**短期规划**（3-6 个月）：
2. Phase 2 - Agent 并行编排（6 周，1.4 倍提升）
   - 核心 Agent 集成
   - 并行执行策略

**中期规划**（6-12 个月）：
3. Phase 4 - 质量门禁增强（4 周，1.13 倍提升）
   - 反屎山总纲自动检查
   - 多层门禁体系

**长期规划**（12+ 个月）：
4. Phase 3 - 持续学习机制（8 周，1.28 倍提升）
   - Instincts 模式提取
   - 自动错误纠正

### 6.3 关键成功因素

1. **渐进式交付**：每个 Phase 独立交付，避免大爆炸式改造
2. **可降级设计**：所有新能力可独立禁用，保持手动流程可用
3. **数据驱动**：持续监控实际效果，根据数据调整优化方向
4. **用户反馈**：定期收集使用反馈，优先解决高频痛点
5. **文档同步**：每个 Phase 完成后更新现行标准件

### 6.4 风险提示

1. **过度工程化**：避免为了自动化而自动化，保持简单实用
2. **维护成本**：自动化系统本身需要维护，评估长期成本
3. **学习曲线**：新能力需要学习成本，提供充分文档和示例
4. **技术债务**：快速迭代可能积累技术债，定期重构清理

---

**最后更新**：2026-04-03  
**下一步行动**：评估 Phase 1 启动条件，准备自动化基础设施开发

# 阶段 1：PreTaskStart（任务启动前）
run_hooks "$HOOKS_DIR/pre-task-start/*.sh"

# 阶段 2：TaskPlanning（任务规划）
run_hooks "$HOOKS_DIR/task-planning/*.sh"

# 阶段 3：PreExecution（执行前）
run_hooks "$HOOKS_DIR/pre-execution/*.sh"

# 阶段 4：Execution（执行中）
run_hooks "$HOOKS_DIR/execution/*.sh"

# 阶段 5：PostExecution（执行后）
run_hooks "$HOOKS_DIR/post-execution/*.sh"

# 阶段 6：PreReview（审查前）
run_hooks "$HOOKS_DIR/pre-review/*.sh"

# 阶段 7：Review（审查中）
run_hooks "$HOOKS_DIR/review/*.sh"

# 阶段 8：PostReview（审查后）
run_hooks "$HOOKS_DIR/post-review/*.sh"

# 阶段 9：PreCommit（提交前）
run_hooks "$HOOKS_DIR/pre-commit/*.sh"

# 阶段 10：Commit（提交中）
run_hooks "$HOOKS_DIR/commit/*.sh"

# 阶段 11：PostCommit（提交后）
run_hooks "$HOOKS_DIR/post-commit/*.sh"
```

**关键特性**：
- 11 个阶段顺序执行，总延迟 < 500ms
- 通过 `hooks-state.json` 共享状态，避免并发冲突
- 支持 Hook 失败时自动降级到手动模式
- 支持 Hook 跳过（通过 `skip-hooks.txt` 配置）

#### 2.2.3 核心 Hooks 设计

**1. anti-shitcode-check.sh（反屎山检查）**

```bash
#!/bin/bash
# 判断型 Hook：检查代码是否符合反屎山总纲

# 读取任务包路径
TASK_DIR=$(jq -r '.task_dir' "$STATE_FILE")

# 检查 6 项强制自检
check_directory_compliance "$TASK_DIR"
check_naming_compliance "$TASK_DIR"
check_boundary_compliance "$TASK_DIR"
check_code_quality "$TASK_DIR"
check_test_coverage "$TASK_DIR"
check_automation_validation "$TASK_DIR"

# 输出结果到状态文件
jq '.anti_shitcode_passed = true' "$STATE_FILE" > "$STATE_FILE.tmp"
mv "$STATE_FILE.tmp" "$STATE_FILE"
```

**2. auto-dispatch-agents.sh（自动分发 Agent）**

```bash
#!/bin/bash
# 自动化 Hook：根据任务复杂度自动分发 Agent

# 读取任务复杂度
COMPLEXITY=$(jq -r '.task_complexity' "$STATE_FILE")

case $COMPLEXITY in
  "low")
    # 低复杂度：只需要 code-reviewer
    dispatch_agent "code-reviewer" "$TASK_DIR"
    ;;
  "medium")
    # 中复杂度：planner + code-reviewer + security-reviewer
    dispatch_agent "planner" "$TASK_DIR"
    dispatch_agent "code-reviewer" "$TASK_DIR"
    dispatch_agent "security-reviewer" "$TASK_DIR"
    ;;
  "high")
    # 高复杂度：全套 Agent 并行
    dispatch_agents_parallel \
      "planner" \
      "architect" \
      "tdd-guide" \
      "code-reviewer" \
      "security-reviewer" \
      "build-fixer" \
      "$TASK_DIR"
    ;;
esac
```

**3. auto-collect-results.sh（自动收集结果）**

```bash
#!/bin/bash
# 自动化 Hook：收集所有 Agent 的输出并合并到任务包

# 等待所有 Agent 完成
wait_for_agents_completion

# 收集结果
collect_agent_results "$TASK_DIR/decision-log.md"
collect_agent_results "$TASK_DIR/result.md"

# 更新状态
update_task_state "$TASK_DIR/state.yaml"
```

### 2.3 Agent 专业分工层设计

#### 2.3.1 Agent 体系设计

参考 ECC 的 36 个 Agent，设计丞相模式的 Agent 体系：

**核心 Agent（10 个）**：
1. **planner** — 任务规划与拆解
2. **architect** — 架构决策与设计
3. **tdd-guide** — 测试驱动开发
4. **code-reviewer** — 代码质量审查
5. **security-reviewer** — 安全漏洞扫描
6. **build-fixer** — 构建错误修复
7. **doc-updater** — 文档更新
8. **refactor-cleaner** — 死代码清理
9. **performance-optimizer** — 性能优化
10. **anti-shitcode-enforcer** — 反屎山总纲执行者（丞相特有）

**扩展 Agent（可选）**：
- e2e-runner — 端到端测试
- database-reviewer — 数据库审查
- api-designer — API 设计
- ...

#### 2.3.2 Agent 调度策略

**串行调度**（低复杂度任务）：
```
planner → tdd-guide → code-reviewer → security-reviewer → 收口
```

**并行调度**（中高复杂度任务）：
```
planner
  ├─ architect ─┐
  ├─ tdd-guide ─┤
  └─ security-reviewer ─┤
                        ├─ 汇总 → code-reviewer → 收口
```

**分层调度**（超高复杂度任务）：
```
第 1 层：planner + architect（规划层）
第 2 层：tdd-guide + code-reviewer + security-reviewer（执行层）
第 3 层：build-fixer + doc-updater（收尾层）
```

#### 2.3.3 Agent 与任务包集成

**Agent 输入**：
- `contract.yaml` — 任务目标、边界、验收标准
- `state.yaml` — 当前状态、风险等级
- `decision-log.md` — 已有决策依据
- `.codex/chancellor/instincts/` — 已学习的模式

**Agent 输出**：
- `decision-log.md` — 追加决策记录
- `result.md` — 追加执行结果
- `state.yaml` — 更新状态
- `gates.yaml` — 触发拍板（如果需要）

**Agent 失败处理**：
- 记录失败原因到 `decision-log.md`
- 更新 `state.yaml` 为 `blocked` 状态
- 触发人工介入 Hook

### 2.4 持续学习层设计

#### 2.4.1 Instincts 存储结构

```yaml
# .codex/chancellor/instincts/coding-pattern-001.yaml
pattern_id: coding-pattern-001
pattern_type: coding
confidence: 0.85
created_at: 2026-04-02T15:30:00Z
updated_at: 2026-04-02T15:30:00Z
source_tasks:
  - v4-trial-010-bootstrap-script-smoke
  - v4-trial-020-panel-acceptance
occurrences: 5

description: |
  在 PowerShell 脚本中读取 JSON 文件时，必须显式指定 UTF-8 编码

pattern:
  trigger: "读取 JSON 文件"
  action: "使用 Get-Content -Encoding UTF8"
  reason: "Windows PowerShell 5.1 默认编码可能导致中文乱码"

example:
  wrong: |
    $json = Get-Content "config.json" | ConvertFrom-Json
  correct: |
    $json = Get-Content "config.json" -Encoding UTF8 | ConvertFrom-Json

validation:
  - 检查是否有 -Encoding UTF8 参数
  - 检查是否处理中文字符
```

#### 2.4.2 模式提取机制

**自动提取触发条件**：
1. 同一错误出现 3 次以上
2. 同一修复方式出现 3 次以上
3. 任务包 `decision-log.md` 中出现"经验教训"关键词

**提取流程**：
```
任务包完成 → 分析 decision-log.md → 识别重复模式 →
生成 Instinct 文件 → 标注置信度 → 存储到 instincts/
```

**模式应用**：
```
任务开始 → 加载相关 Instincts → 注入到 Agent 上下文 →
Agent 执行时应用模式 → 任务结束后更新 Instinct 置信度
```

#### 2.4.3 团队规范提取

**`传令：提取规范` 命令**：

```bash
# 分析 Git 历史
git log --all --pretty=format:"%h %s" --since="3 months ago" > commits.txt

# 提取提交规范
analyze_commit_messages commits.txt > team-commit-rules.md

# 提取代码规范
analyze_code_patterns ./ > team-code-rules.md

# 提取测试规范
analyze_test_patterns ./ > team-test-rules.md

# 合并为团队规范
merge_rules team-*-rules.md > .codex/chancellor/team-rules.md
```

### 2.5 质量门禁层增强

#### 2.5.1 反屎山总纲集成

**优先级**：反屎山总纲 > Agent 建议 > Instincts 模式

**执行流程**：
```
代码生成 → 反屎山总纲检查 → 不通过 → 拒绝输出
                           → 通过 → Agent 审查 → 不通过 → 修复建议
                                                → 通过 → 提交
```

#### 2.5.2 多层审查机制

**第 1 层：反屎山总纲（100% 合规）**
- 目录锁死检查
- 命名规范检查
- 边界锁死检查
- 代码质量检查
- 测试覆盖率检查
- 自动化校验检查

**第 2 层：Agent 专业审查**
- code-reviewer：架构、可维护性
- security-reviewer：安全漏洞
- anti-shitcode-enforcer：反屎山细节

**第 3 层：Instincts 模式匹配**
- 检查是否违反已学习的模式
- 检查是否重复历史错误

## 三、实施路线图

### Phase 1：Hooks 编排层（4 周）

**Week 1-2：核心 Hooks 开发**
- 开发 dispatcher.sh 分发器
- 开发 20 个核心 Hooks（10 判断型 + 10 自动化）
- 集成到现有任务包流程

**Week 3：Hooks 测试与优化**
- 至少 3 个真实任务验证
- 性能优化（延迟 < 500ms）
- 失败降级机制测试

**Week 4：Hooks 文档与培训**
- 编写 Hooks 开发指南
- 编写 Hooks 使用手册
- 更新任务包模板

**预期收益**：
- 减少 70% 人工干预
- 自动化任务包创建、状态更新、结果收集
- 效率提升 3 倍

### Phase 2：Agent 专业分工层（6 周）

**Week 5-6：核心 Agent 集成**
- 集成 ECC 的 4 个核心 Agent（planner/tdd-guide/code-reviewer/security-reviewer）
- 开发 Agent 调度引擎
- 适配任务包输入输出

**Week 7-8：Agent 并行调度**
- 开发并行调度机制
- 开发结果汇总机制
- 开发失败处理机制

**Week 9-10：扩展 Agent 系统**
- 新增 6 个专业 Agent
- 开发 anti-shitcode-enforcer（丞相特有）
- 优化 Agent 上下文管理

**预期收益**：
- 并行处理提升效率 2 倍
- 专业 Agent 保证质量稳定
- 支持复杂任务自动拆解

### Phase 3：持续学习层（4 周）

**Week 11-12：Instincts 系统**
- 设计 Instincts 存储结构
- 开发模式提取引擎
- 开发模式应用机制

**Week 13：团队规范提取**
- 开发 `传令：提取规范` 命令
- 分析 Git 历史提取规范
- 生成团队规范文档

**Week 14：持续优化**
- Instincts 置信度自动更新
- 错误模式自动纠正
- 团队规范自动同步

**预期收益**：
- 自动纠正 70% 重复错误
- 降低规则维护成本 50%
- 团队规范自动共享

### Phase 4：质量门禁增强（2 周）

**Week 15：多层审查集成**
- 反屎山总纲 + Agent 审查集成
- Instincts 模式匹配集成
- 优先级冲突解决机制

**Week 16：全流程测试**
- 至少 10 个真实任务验证
- 性能压测（支持 5 个并行任务）
- 稳定性测试（7×24 小时运行）

**预期收益**：
- 质量更稳定（多层审查）
- 零漏网之鱼（反屎山总纲 + Agent + Instincts）
- 效率提升 1.5 倍

## 四、成功标准

### 4.1 Phase 1 成功标准（4 周后）

- ✅ 20 个核心 Hooks 开发完成并稳定运行
- ✅ Hooks 延迟 < 500ms
- ✅ 至少 3 个真实任务使用 Hooks 完成
- ✅ 人工干预减少 70%

### 4.2 Phase 2 成功标准（10 周后）

- ✅ 10 个专业 Agent 稳定运行
- ✅ 支持 3-5 个 Agent 并行处理
- ✅ 至少 10 个真实任务使用 Agent 完成
- ✅ 效率提升 6 倍（3 × 2）

### 4.3 Phase 3 成功标准（14 周后）

- ✅ Instincts 系统积累至少 30 个有效模式
- ✅ 自动纠正 70% 重复错误
- ✅ 团队规范自动提取准确率 80%+
- ✅ 效率提升 12 倍（3 × 2 × 2）

### 4.4 Phase 4 成功标准（16 周后）

- ✅ 多层审查零漏网之鱼
- ✅ 支持 5 个并行任务
- ✅ 7×24 小时稳定运行
- ✅ **效率提升 13.5 倍（3 × 2 × 2 × 1.5 × 1.5）**

## 五、风险与应对

### 5.1 技术风险

**风险 1：Hooks 性能瓶颈**
- 风险等级：⭐⭐⭐
- 应对：异步执行非关键 Hooks，缓存状态文件

**风险 2：Agent 并行冲突**
- 风险等级：⭐⭐⭐
- 应对：文件锁机制，冲突时自动降级到串行

**风险 3：Instincts 错误模式**
- 风险等级：⭐⭐
- 应对：人工审查低置信度模式，支持模式禁用

### 5.2 质量风险

**风险 4：Agent 绕过反屎山总纲**
- 风险等级：⭐⭐⭐⭐
- 应对：反屎山总纲优先级最高，Agent 输出必须通过检查

**风险 5：自动化导致质量下降**
- 风险等级：⭐⭐⭐
- 应对：保持人工审查关键节点，支持自动化降级

### 5.3 维护风险

**风险 6：Hooks/Agent 维护成本高**
- 风险等级：⭐⭐⭐
- 应对：优先集成成熟组件，建立测试套件

**风险 7：系统复杂度增加**
- 风险等级：⭐⭐⭐
- 应对：保持向后兼容，支持渐进式启用

## 六、总结

### 6.1 核心创新

1. **Hooks 编排层**：借鉴 Claw-code 的 84 个 Hooks，实现自动化编排
2. **Agent 专业分工**：借鉴 ECC 的 36 个 Agent，实现并行处理
3. **持续学习**：借鉴 ECC 的 Instincts，实现自动优化
4. **质量铁律**：保持丞相的反屎山总纲，实现 100% 合规
5. **任务持久化**：保持丞相的任务包体系，实现跨轮恢复

### 6.2 预期效果

- **效率提升**：13.5 倍（3 × 2 × 2 × 1.5 × 1.5）
- **质量提升**：多层审查，零漏网之鱼
- **维护成本**：降低 50%（自动学习、自动优化）
- **团队协作**：支持 3-5 人小团队
- **可扩展性**：支持 100+ Hooks、50+ Agents

### 6.3 下一步行动

**立即可做**：
1. 评估 Phase 1 启动时间（4 周投入）
2. 设计核心 20 个 Hooks 的详细规格
3. 准备 Agent 集成环境

**1 周内**：
1. 启动 Phase 1：Hooks 编排层开发
2. 开发 dispatcher.sh 分发器
3. 开发前 5 个核心 Hooks

**4 周内**：
1. 完成 Phase 1 开发
2. 至少 3 个真实任务验证
3. 评估是否进入 Phase 2

---

**参考资料**

- [丞相模式与ECC功能对比分析](./32-丞相模式与ECC功能对比分析.md)
- [ECC功能采纳建议与实施路线图](./33-ECC功能采纳建议与实施路线图.md)
- [Claw Code Architecture](https://blakecrosley.com/blog/anatomy-of-a-claw)
- [OpenClaw Architecture](https://ppaolo.substack.com/p/openclaw-system-architecture-overview)
- [反屎山AI研发执行总纲](../reference/01-反屎山AI研发执行总纲（Codex专用浓缩对照版）.md)
