# V4 核心治理规则真源门禁记录

时间：2026-03-29 14:24:54
任务：v4-trial-032-core-rule-source-gate

## 动作

1. 识别长期维护瓶颈：必需规则文件存在性与公开规则入口同步仍由两份硬编码数组维护，未来容易一处改了另一处忘改。
2. 扩展 `.codex/chancellor/invoke-public-commit-governance-gate.ps1`，把 `docs/40-执行/10-本地安全提交流程.md` 的“核心治理规则入口真源”区块升级为单点真源，同时驱动“必需规则文件存在性”与“公开规则入口同步”。
3. 扩展 `.codex/chancellor/test-public-commit-governance-gate.ps1`，新增“核心治理规则真源区块缺失必须失败”和“同步改动真源与公开入口时允许通过”的场景。
4. 同步更新 `.codex/chancellor/README.md`、`docs/40-执行/10-本地安全提交流程.md`、`docs/30-方案/08-V4-治理审计候选规范.md`，明确核心治理规则入口已改为由安全提交流程区块供给真源。

## 结果

- 当前自动门禁已把核心治理规则入口从双份硬编码提升为“安全提交流程 section 真源”。
- 当前仓现状通过新门禁与测试。
- 人为破坏核心治理规则真源区块时，门禁会给出明确阻断信息；若同步改动真源与公开入口，门禁允许通过。

## 理由

- 同一套规则口径若长期靠两份数组维护，哪怕今天都对，后续也迟早会分叉。
- 直接把规则清单挂到现行文档区块，短期实现更难，但长期最稳、最轻，也最符合 V8 的复利目标。

## 下一步

1. 暂存本轮公开安全改动并执行暂存区门禁。
2. 完成 `commit`、`pull --rebase origin main` 与 `push origin main`。
3. 回写任务包状态、本地日志并清空活动指针。
