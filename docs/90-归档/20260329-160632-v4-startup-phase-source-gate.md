# V4 启动阶段真源硬门禁记录

时间：2026-03-29 16:06:32
任务：第三十九刀（启动阶段真源硬门禁）

## 动作

1. 复盘公开提交治理门禁当前借道关系，确认启动阶段顺序已切到 `docs/00-导航/01-V4-重启导读.md` 的“启动阶段真源”区块，但现有 helper 只校验首尾边界，删除中间关键项时仍会静默放行。
2. 在 `.codex/chancellor/invoke-public-commit-governance-gate.ps1` 新增通用断言 `Assert-RequiredOrderedPathsPresent`，用于校验真源区块中的必需路径集合与相对顺序。
3. 让 `Get-CanonicalStartupPhaseEntryPaths` 在完成切片后，强制校验启动阶段最小必需集合：`02-现行标准件总览`、`01-V4-重启导读`、`01-V4-重启ADR`、`01-旧仓必需资产清单`、`01-V4-最小目录蓝图`、`02-V4-目录锁定清单`、`03-V4-MVP边界清单`。
4. 保持 `README.md` 与 `docs/README.md` 继续以“启动阶段真源”为唯一公开口径来源，不把真源重新借回 `先看什么` 或其他展示区块。
5. 复跑 `.codex/chancellor/test-public-commit-governance-gate.ps1`，确认新增的 `block-startup-phase-source-boundary-missing` 用例已由失败转为通过。
6. 复跑当前工作集门禁，确认本轮改动在真实 changed paths 上可通过公开提交治理门禁。

## 结果

- 当前自动门禁已从“只认首尾”升级为“首尾 + 启动阶段最小必需集合”双重硬校验。
- 删除 `docs/20-决策/01-V4-重启ADR.md` 这类启动阶段关键中项时，门禁会自动失败，不再需要人工复读提醒。
- 启动阶段顺序真源仍集中在 `docs/00-导航/01-V4-重启导读.md` 的专用区块，公开首页只负责与其保持一致。
- 第三十九刀对应总回归与当前工作集门禁均已通过。

## 理由

- 启动阶段列表属于高频入口、低容错约束，若只校验切片首尾，中间关键项被删掉时会形成“真源自我缩水”且无自动告警。
- 用通用必需路径断言补上硬约束，可以让真源保持集中，又避免把长期秩序再次退回到人工巡检。

## 下一步

1. 暂存本轮公开安全改动并执行暂存区门禁。
2. 完成 `commit`、`pull --rebase origin main` 与 `push origin main`。
3. 下一刀继续寻找仍存在“真源区块自身缺项可静默漏过”的公开口径一致性入口。