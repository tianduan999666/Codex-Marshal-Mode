# V4 重启导读核心顺序门禁记录

时间：2026-03-29 14:34:04
任务：v4-trial-033-restart-guide-core-order-gate

## 动作

1. 识别长期维护瓶颈：两个公开首页对重启导读核心入口目前只校验“存在”，尚未校验完整顺序，非启动阶段的漂移仍可能漏过。
2. 扩展 `.codex/chancellor/invoke-public-commit-governance-gate.ps1`，把 `README.md` 与 `docs/README.md` 对重启导读核心入口的校验升级为“存在性 + 顺序”，并继续以 `docs/00-导航/01-V4-重启导读.md` 的“先看什么”区块为真源。
3. 扩展 `.codex/chancellor/test-public-commit-governance-gate.ps1`，新增“交换任务包规范 / 任务包模板顺序时必须失败”的场景，证明门禁已覆盖启动阶段之外的重启导读核心顺序漂移。
4. 同步更新 `.codex/chancellor/README.md`、`docs/40-执行/10-本地安全提交流程.md`、`docs/30-方案/08-V4-治理审计候选规范.md`，明确两个公开首页现已校验重启导读核心入口的存在性与顺序。

## 结果

- 当前自动门禁已把两个公开首页对重启导读核心入口的校验从“仅存在性”升级为“存在性 + 全序列顺序”。
- 当前仓现状通过新门禁与测试。
- 人为调换非启动阶段的重启导读核心入口顺序时，门禁会自动失败。

## 理由

- 若只校验存在性，后续公开首页把顺序改乱了也不会被及时阻断，长期会损耗导航信任度。
- 直接以重启导读“先看什么”区块驱动完整顺序门禁，短期实现更难，但长期最稳、最省心。

## 下一步

1. 暂存本轮公开安全改动并执行暂存区门禁。
2. 完成 `commit`、`pull --rebase origin main` 与 `push origin main`。
3. 回写任务包状态、本地日志并清空活动指针。
