# V4 维护层动作矩阵表格门禁落地记录

时间：2026-03-31 12:04:35
任务：第一百三十八刀（维护层动作矩阵表格本体硬门禁）

## 动作

1. 在 `.codex/chancellor/invoke-public-commit-governance-gate.ps1` 增补 Markdown 表格行解析，并新增 `Get-CanonicalMaintenanceMatrixRows`，把 `docs/40-执行/14-维护层动作矩阵与收口检查表.md` 的矩阵表头、分隔行、动作顺序与 12 条行级内容全部纳入硬门禁。
2. 在 `.codex/chancellor/test-public-commit-governance-gate.ps1` 增补表头与前两行前置条件，并补两类新负例：风险级别漂移、动作顺序漂移。
3. 更新 `docs/30-方案/09-V4-本机生产切换最小闭环方案.md`，把本轮“维护层动作矩阵表格本体已下沉门禁”的现状回写到方案区。
4. 执行显式门禁、两条定向负例验证，并尝试执行整套治理回归。

## 理由

- `14-维护层动作矩阵与收口检查表.md` 的表格本体是维护层判路控制面；只锁摘要层还不够，真正决定动作入口、最低产出、公开边界与风险级别的是矩阵行本身。
- 先把表格本体做成顺序矩阵，后续无论继续自动化还是人工维护，都可以直接复用同一控制面，长期收益高于继续补更浅层描述。

## 结果

- 维护层动作矩阵表格本体已纳入公开提交治理门禁，覆盖表头、分隔行、动作顺序与 12 条行级内容。
- 显式门禁通过：`docs/40-执行/14-维护层动作矩阵与收口检查表.md` 在现状下可正常放行。
- 定向负例通过：
  - 把“本地安全提交”的 `风险级别` 从 `低` 改成 `中` 时，门禁按预期失败。
  - 交换“本地安全提交 / 任务包半自动起包”两行顺序时，门禁按预期失败。
- 整套 `.codex/chancellor/test-public-commit-governance-gate.ps1` 未能完成收口；当前仓内存在与本轮无关的既有基线漂移：`docs/40-执行/03-面板入口验收.md` 中“公开边界”子项实际为“必要时也可以推荐终端别名”，与门禁期望不一致。

## 下一步

1. 若继续治理同链路，优先处理 `docs/40-执行/03-面板入口验收.md` 的既有漂移，使整套治理回归重新回到全绿。
2. 在 `14-维护层动作矩阵与收口检查表.md` 继续盘点仍未门禁化的自由描述层，但不跨出当前矩阵控制面主线。
