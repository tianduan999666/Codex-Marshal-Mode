# V4 面板帮助命令固定结构门禁落地记录

时间：2026-03-30 00:46:11
任务：第七十刀（`丞相帮助` 三段式固定结构硬门禁）

## 动作

1. 在 `docs/40-执行/03-面板入口验收.md` 为 `丞相帮助` 新增“帮助命令固定结构”区块，固定为：
   - `当前用法`：先说明当前默认入口与适用场景
   - `面板命令`：再列出当前面板丞相命令及其含义
   - `注意事项`：最后提示维护层动作、安全边界与人工验板提醒
2. 同步更新 `docs/40-执行/03-面板入口验收.md` 的通过标准与失败信号，使其不仅校验“有帮助”，也校验“有固定结构”。
3. 在 `codex-home-export/panel-acceptance-checklist.md` 新增同名区块，使人工验板清单与入口验收文档共享同一套帮助模板。
4. 在 `.codex/chancellor/invoke-public-commit-governance-gate.ps1` 扩展 `Get-CanonicalPanelCommandState`：
   - 入口验收文档中的帮助命令固定结构必须与真源模板一致
   - 人工验板清单中的帮助命令固定结构必须与真源模板一致
5. 在 `.codex/chancellor/test-public-commit-governance-gate.ps1` 增补回归测试，覆盖：
   - 面板入口验收文档帮助模板漂移被拦截
   - 人工验板清单帮助模板漂移被拦截
6. 更新 `docs/30-方案/09-V4-本机生产切换最小闭环方案.md`，把本轮治理沉淀回当前现状。

## 结果

- `丞相帮助` 已从“有一句定义”继续升级为“有固定三段式模板”。
- 当前面板治理链已覆盖：命令名、帮助用法、帮助结构、核心响应口径、维护边界、验板顺序。
- 若未来有人把帮助页改成无序散文而不回写真源，提交前会被硬门禁拦截。

## 下一步

1. 继续评估是否需要把“注意事项”内部也细化为固定子项，例如安全边界、维护层动作、新开会话验板提醒。
2. 主公下次新开官方面板会话时，仍按 `codex-home-export/panel-acceptance-checklist.md` 做人工验板，即可补完当前桥接切换的人眼确认。
