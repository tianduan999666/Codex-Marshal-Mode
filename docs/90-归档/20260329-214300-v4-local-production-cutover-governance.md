# V4 本机生产切换线首刀治理补口记录

时间：2026-03-29 21:43:00
任务：第六十刀（本机生产切换线治理前置）

## 动作

1. 复盘当前“边开发边用”与“新仓接管本机生产”之间的真实断点。
2. 确认当前仓还没有 `codex-home-export/`、`VERSION.json` 与单机切换/回滚闭环，不能直接宣称已能丝滑切生产。
3. 新增 `docs/reference/root-layout-governance.md`，先把顶层新增治理与 `codex-home-export/` 候选职责写清。
4. 新增 `docs/30-方案/09-V4-本机生产切换最小闭环方案.md`，冻结本线的最小目标、非目标、四件套与推进顺序。

## 结果

- 当前仓已补上“本机生产切换线”的治理前置。
- 后续若要新建 `codex-home-export/`，已有明确职责、边界与最小闭环设计，不再是临场拍脑袋扩根目录。
- 本轮故意不直接上生产脚本，避免在母体职责、回滚口径未冻结前先写出高耦合脚本。

## 下一步

1. 更新目录锁定清单与最小目录蓝图，正式批准 `codex-home-export/` 顶层位。
2. 落第一版生产母体骨架：`README.md`、`VERSION.json`、`manifest.json`。
3. 再补单机 `install-to-home` 与回滚脚本。