# V4 关键规则入口同步门禁记录

时间：2026-03-29 12:33:52
任务：v4-trial-023-public-rule-entry-sync-gate

## 动作

1. 识别当前下一层公开口径瓶颈是 `docs/reference` 与核心治理规则在三处公开入口之间仍可能漏挂。
2. 先扩测试脚本，新增“人为删掉 docs/README 中关键规则入口时必须阻断”的漂移场景。
3. 扩展 `invoke-public-commit-governance-gate.ps1`，把 `README.md`、`docs/README.md`、`docs/00-导航/02-现行标准件总览.md` 三处对关键规则入口的同步纳入自动校验。
4. 测试过程中发现 `docs/README.md` 确实漏挂 `30-方案/02-V4-目录锁定清单.md`，已同步补齐。

## 结果

- 当前自动门禁已从“执行区入口同步”继续扩到“关键规则入口同步”。
- 当前仓现状可通过新门禁；人为删掉 docs/README 的关键规则入口时，门禁会自动失败。
- `docs/README.md` 现已补齐 `目录锁定清单` 入口，公开口径更完整。

## 理由

- 公开口径漂移不仅发生在执行区目录，也发生在“规则入口有没有挂齐”。
- 把关键规则入口同步下沉到门禁，能更早拦住“规则存在但入口丢失”的长期隐患。
- 这一步依旧保持当前目录内自含，不新增目录、不引入依赖。

## 下一步

1. 暂存本轮公开安全改动并跑真实门禁。
2. 完成本轮 `commit`、`pull --rebase`、`push`。
3. 回写任务包状态并清空活动指针。
