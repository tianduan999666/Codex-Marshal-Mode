# V4 面板人工验板收口提示增强记录

时间：2026-03-31 20:58:45
任务：第一百五十三刀（面板人工验板收口提示增强）

## 动作

1. 增强 `codex-home-export/verify-panel-acceptance-result.ps1`，在结果稿格式复核通过后，直接输出 `FinalResult`、`NeedRollback`、`NeedFollowup`、`NextAction` 等收口提示。
2. 为不通过分支补充明确的人话建议：若需要回退，则提示先执行 `verify-cutover.ps1`，仍异常再执行 `rollback-from-backup.ps1`；若无需回退，则提示先补最小缺口再重验。
3. 修正结果稿字段解析的根因问题：空白字段原先会误吞下一条 bullet，现已改为只匹配当前行内空白，避免“若不通过，最小缺口是”串到“下一步”。
4. 更新 `codex-home-export/README.md`，明确结果复核脚本现在会直接给出收口提示。
5. 用一条通过样例和一条不通过样例做双向验证，确认两种结论都能输出正确提示。

## 理由

- 现有链路已经能起稿和复核，但复核通过后仍需要人脑再翻译“下一步该怎么做”，最后一公里还有摩擦。
- 更关键的是，字段解析存在隐藏串行风险；若不先修根，后续基于结果稿做任何自动化都不稳。

## 结果

- 通过态结果稿现在会直接提示：结果稿已可作为人工验板证据之一，下一步进入维护层统一收口剩余非终态任务。
- 不通过态结果稿现在会直接提示：当前不能按通过态收口，并根据是否需要回退给出明确动作。
- `若不通过，最小缺口是` 的空白字段串行问题已修正，不再误读下一条 `下一步`。

## 下一步

1. 主公完成真实人工验板后，直接运行 `verify-panel-acceptance-result.ps1 -ResultPath <结果稿>`，即可拿到格式复核结果与收口提示。
2. 若真实结果为通过，我下一刀就按结果稿证据统一收口剩余非终态任务。
3. 若真实结果为不通过，就按脚本提示先补最小缺口或回退，再重新验板。
