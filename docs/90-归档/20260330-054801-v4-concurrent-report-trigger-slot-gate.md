# V4 复杂并存汇报触发条件槽位门禁落地记录

时间：2026-03-30 05:48:01
任务：第一百零一刀（复杂并存汇报什么时候用固定槽位硬门禁）

## 动作

1. 在 `docs/40-执行/20-复杂并存汇报骨架模板.md` 新增 `什么时候用固定槽位` 区块。
2. 在 `.codex/chancellor/invoke-public-commit-governance-gate.ps1` 新增固定槽位校验，把 `20` 文档中的触发条件真源纳入公开提交治理门禁。
3. 在 `.codex/chancellor/test-public-commit-governance-gate.ps1` 增补回归测试，覆盖触发条件固定槽位漂移拦截。
4. 更新 `docs/30-方案/09-V4-本机生产切换最小闭环方案.md`，把本轮治理沉淀回当前现状。

## 理由

- `什么时候用` 直接决定复杂并存汇报何时被触发，如果继续停留在散列条目，长期容易被执行者按经验提前、延后或跳过。
- 把触发条件收敛为固定槽位后，复杂并存模板、测试脚本与公开提交治理门禁可以共用同一真源，能把触发边界持续钉死。

## 结果

- `什么时候用` 已从散列条目收敛为固定槽位模板。
- `20-复杂并存汇报骨架模板.md` 与公开提交治理门禁已共用同一套触发条件真源。
- `已选主状态 → 同步复杂裁决 → 超出单一模板` 已可被自动门禁与回归脚本稳定拦截漂移。

## 下一步

1. 继续盘点 `20-复杂并存汇报骨架模板.md` 中仍偏经验口径、尚未固定槽位化的区块。
2. 若 `20` 文档继续推进收益下降，则转向 `19-多 gate 与多异常并存处理规则.md` 的高频公开口径区块，继续沿用同一硬门禁模式下沉。
