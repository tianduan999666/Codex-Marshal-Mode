# V4 目录锁定清单真源化白名单记录

时间：2026-03-29 14:58:30
任务：v4-trial-034-public-rule-order-gate

## 动作

1. 复盘上一轮失败方向，确认“公开首页规则顺序门禁”会与现有重启导读核心顺序、Target 主线顺序产生语义冲突，停止继续扩展该方向。
2. 在 `docs/30-方案/02-V4-目录锁定清单.md` 新增“公开仓允许跟踪的运行态文件”区块，把公开仓允许跟踪的 `.codex` 文件白名单收敛为真源。
3. 扩展 `.codex/chancellor/invoke-public-commit-governance-gate.ps1`，把受控顶层跟踪面与公开仓允许跟踪的 `.codex` 运行态文件白名单，从脚本硬编码改为读取目录锁定清单。
4. 扩展 `.codex/chancellor/test-public-commit-governance-gate.ps1`，新增“删除顶层批准项即失败”“删除允许跟踪的 `.codex` 文件即失败”两条回归测试。
5. 同步更新 `.codex/chancellor/README.md`、`docs/40-执行/10-本地安全提交流程.md`、`docs/30-方案/08-V4-治理审计候选规范.md`，明确目录锁定清单已成为相关白名单真源。

## 结果

- 当前自动门禁已不再双处硬编码受控顶层跟踪面与公开仓允许跟踪的 `.codex` 文件白名单。
- 删除目录锁定清单中的批准顶层项或允许跟踪的 `.codex` 文件时，门禁会自动失败。
- 当前仓现状通过回归测试，第三十四刀方向已转入长期更稳的真源化治理路径。

## 理由

- 受控顶层跟踪面与公开仓允许跟踪的运行态文件本质上都属于目录治理边界，放回目录锁定清单才符合单一真源原则。
- 短期改脚本更省事，但会继续制造规则漂移点；短期多做一步真源化，长期维护更轻、更稳。

## 下一步

1. 暂存本轮公开安全改动并执行暂存区门禁。
2. 完成 `commit`、`pull --rebase origin main` 与 `push origin main`。
3. 回写任务包状态、本地日志并清空活动指针。
