# V4 主线真源完整性硬门禁记录

时间：2026-03-29 16:17:27
任务：第四十刀（主线真源完整性硬门禁）

## 动作

1. 复盘上一刀后的剩余同类漏洞，确认 `docs/40-执行/12-V4-Target-实施计划.md` 与 `docs/40-执行/13-维护层总入口.md` 的真源 helper 仍只靠首尾或公开入口反校验，删除中间关键项时会静默漏过。
2. 在 `.codex/chancellor/invoke-public-commit-governance-gate.ps1` 把 Target 主线真源、维护层主线真源都补入“必需路径存在性”硬断言，并把启动阶段真源同步收敛到同一套断言模型。
3. 保持顺序定义仍由各自真源区块负责，门禁只硬卡“必需集合不能缩水”，避免重新把真源顺序硬编码回脚本本体。
4. 在 `.codex/chancellor/test-public-commit-governance-gate.ps1` 新增两个回归：一条删除 Target 主线真源中的 `07-V4-规划策略候选规范.md`，一条删除维护层主线真源中的 `19-多 gate 与多异常并存处理规则.md`，均要求门禁失败。
5. 同步更新 `.codex/chancellor/README.md`、`docs/40-执行/10-本地安全提交流程.md`、`docs/30-方案/08-V4-治理审计候选规范.md`，明确门禁已阻断 Target / 维护层 / 启动阶段真源自身缺少关键项。

## 结果

- 当前自动门禁已补齐三条有序真源的“完整性硬门禁”：启动阶段、Target 主线、维护层主线。
- 删除这三类真源中的关键中项时，门禁会自动失败，不再依赖人工复查。
- 真源顺序定义仍留在专属文档区块本身；只要真源与公开入口整体联动更新，门禁不会被脚本内的重复顺序定义卡死。
- 总回归与当前工作集门禁均已通过。

## 理由

- 真源区块如果只校验边界，长期一定会出现“中间项缩水但无人知晓”的隐性漂移。
- 只把“必需集合存在性”硬编码进门禁，能同时满足两件事：一是关键项不能悄悄消失，二是真源仍是顺序唯一来源，长期维护成本更低。

## 下一步

1. 暂存本轮公开安全改动并执行暂存区门禁。
2. 完成 `commit`、`pull --rebase origin main` 与 `push origin main`。
3. 下一刀继续查找仍未下沉到自动门禁的公开口径一致性约束。