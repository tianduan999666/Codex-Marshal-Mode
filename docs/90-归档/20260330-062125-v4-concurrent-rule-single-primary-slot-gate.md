# V4 多 gate 并存单任务单主状态槽位门禁落地记录

时间：2026-03-30 06:21:25
任务：第一百零五刀（多 gate 与多异常并存处理规则单任务单主状态固定槽位硬门禁）

## 动作

1. 在 `docs/40-执行/19-多 gate 与多异常并存处理规则.md` 新增 `第一原则：单任务单主状态固定槽位` 区块。
2. 在 `.codex/chancellor/invoke-public-commit-governance-gate.ps1` 新增固定槽位校验，并将其挂入预计算链。
3. 在 `.codex/chancellor/test-public-commit-governance-gate.ps1` 增补回归测试，覆盖单任务单主状态固定槽位漂移拦截。
4. 更新 `docs/30-方案/09-V4-本机生产切换最小闭环方案.md`，把本轮治理沉淀回当前现状。

## 理由

- `单任务单主状态` 是复杂并存场景的第一道边界；如果继续停留在经验口径，最容易把 `state.yaml` 写成多主状态，或让次要事项争抢主状态表达。
- 先把 `单任务单主状态` 收敛为固定槽位，再下沉到同一公开提交治理门禁，可以把复杂并存场景的主状态边界长期钉死。

## 结果

- `单任务单主状态` 已从散列规则收敛为固定槽位模板。
- `19-多 gate 与多异常并存处理规则.md` 与公开提交治理门禁已共用同一套单任务单主状态真源。
- `state.yaml 单主状态 → 次要事项留档 → 主阻塞唯一` 已可被自动门禁与回归脚本稳定拦截漂移。

## 下一步

1. 继续盘点 `19-多 gate 与多异常并存处理规则.md` 中仍偏经验口径、尚未固定槽位化的区块。
2. 优先考虑把 `第二原则：按下一行动主体决定优先级` 或 `第三原则：多个 gate 并存时的主次划分` 继续沿同一硬门禁模式下沉。
