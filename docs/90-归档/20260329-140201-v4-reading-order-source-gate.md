# V4 现行总览阅读顺序真源门禁记录

时间：2026-03-29 14:02:01
任务：v4-trial-030-reading-order-source-gate

## 动作

1. 识别长期维护瓶颈：Target / 维护层顺序虽然已进入公开门禁，但仍由脚本硬编码数组维护，未来容易与现行总览“阅读顺序建议”区块再次分叉。
2. 扩展 `.codex/chancellor/invoke-public-commit-governance-gate.ps1`，把 `docs/00-导航/02-现行标准件总览.md` 的“阅读顺序建议”区块升级为 Target / 维护层顺序真源，并新增边界缺失时的友好阻断消息。
3. 扩展 `.codex/chancellor/test-public-commit-governance-gate.ps1`，新增“真源边界缺失必须失败”和“同步改动真源及公开首页时允许通过”的场景。
4. 同步更新 `.codex/chancellor/README.md`、`docs/40-执行/10-本地安全提交流程.md`、`docs/30-方案/08-V4-治理审计候选规范.md`，明确 Target / 维护层顺序已改为由现行总览区块供给真源。

## 结果

- 当前自动门禁已把 Target / 维护层顺序从脚本硬编码提升为“现行总览阅读顺序 section 真源”。
- 当前仓现状通过新门禁与测试。
- 人为破坏真源边界时，门禁会给出明确阻断信息；若同步改动现行总览与两个公开首页，门禁允许通过。

## 理由

- 真源若继续留在脚本里，每次文档顺序微调都要同步改脚本，长期必然漂移。
- 直接以现行文档区块驱动公开门禁，短期实现更难，但长期最稳、最省心，也最符合 V8 的复利目标。

## 下一步

1. 暂存本轮公开安全改动并执行暂存区门禁。
2. 完成 `commit`、`pull --rebase origin main` 与 `push origin main`。
3. 回写任务包状态、本地日志并清空活动指针。
