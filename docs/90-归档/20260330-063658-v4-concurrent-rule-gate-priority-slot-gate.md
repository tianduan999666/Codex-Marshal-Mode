# V4 多 gate 并存主次划分槽位门禁落地记录

时间：2026-03-30 06:36:58
任务：第一百零七刀（多 gate 与多异常并存处理规则多个 gate 并存时的主次划分固定槽位硬门禁）

## 动作

1. 在 `docs/40-执行/19-多 gate 与多异常并存处理规则.md` 新增 `第三原则：多个 gate 并存时的主次划分固定槽位` 区块。
2. 在 `.codex/chancellor/invoke-public-commit-governance-gate.ps1` 新增固定槽位校验，并将其挂入预计算链。
3. 在 `.codex/chancellor/test-public-commit-governance-gate.ps1` 增补回归测试，覆盖多个 gate 并存时的主次划分固定槽位漂移拦截。
4. 更新 `docs/30-方案/09-V4-本机生产切换最小闭环方案.md`，把本轮治理沉淀回当前现状。

## 理由

- `多个 gate 并存时的主次划分` 决定复杂并存场景如何选主 gate、如何保留次要事项；如果继续停留在经验口径，最容易把主 gate 与次要 gate 说乱。
- 先把这层收敛为固定槽位，再下沉到同一公开提交治理门禁，可以把复杂并存场景的主次裁决顺序长期钉死。

## 结果

- `多个 gate 并存时的主次划分` 已从散列条目收敛为固定槽位模板。
- `19-多 gate 与多异常并存处理规则.md` 与公开提交治理门禁已共用同一套主次划分真源。
- `主 gate 唯一 → 直接阻断优先 → 影响范围次级 → 截止时间兜底 → 次要待处理项留档` 已可被自动门禁与回归脚本稳定拦截漂移。

## 下一步

1. 继续盘点 `19-多 gate 与多异常并存处理规则.md` 中仍偏经验口径、尚未固定槽位化的区块。
2. 优先考虑把 `第四原则：gate 与异常并存时的裁决` 继续沿同一硬门禁模式下沉。
