# V4 自动验板脚本下一步提示落地记录

时间：2026-03-31 18:38:40
任务：第一百四十四刀（自动验板后的人工验板提示下沉）

## 动作

1. 更新 `codex-home-export/verify-cutover.ps1`，在自动验板通过后直接打印下一步人工验板动作。
2. 将人工验板顺序固定为“`丞相版本` → `丞相检查` → `丞相状态`（必要时）”。
3. 将异常回退顺序固定为“先重跑 `verify-cutover.ps1`，仍异常再执行 `rollback-from-backup.ps1`”。
4. 重新执行 `codex-home-export/verify-cutover.ps1`，确认原有校验与新增提示同时生效。

## 理由

- 当前最小闭环的主瓶颈已经收敛为人工验板，继续补大块文档收益不如直接降低最后一步的操作负担。
- 把“下一步做什么”直接下沉到自动验板脚本，能减少使用者记忆成本，避免验板时回头翻文档。

## 结果

- 自动验板通过后，脚本会直接提示“打开官方面板、新开会话、按顺序输入命令”。
- 若人工验板发现漂移，脚本提示也会同步给出最小回退路径。
- 当前从“自动验板通过”走到“人工验板开始”的路径更短、更稳、更容易执行。

## 下一步

1. 进入官方面板，按脚本提示执行人工验板。
2. 若人工验板仍有漂移，再按最小缺口补 `1` 刀到 `2` 刀并立即回归。
