# V4 面板帮助命令当前用法与面板命令子项门禁落地记录

时间：2026-03-30 01:12:38
任务：第七十二刀（`丞相帮助` 当前用法 / 面板命令固定子项硬门禁）

## 动作

1. 在 `docs/40-执行/03-面板入口验收.md` 新增“当前用法固定子项”与“面板命令固定子项”区块。
2. 同步更新 `docs/40-执行/03-面板入口验收.md` 的通过标准与失败信号，使其明确约束帮助页的两层新模板。
3. 在 `codex-home-export/panel-acceptance-checklist.md` 落同名区块，让人工验板清单继续与公开验收文档对齐。
4. 在 `.codex/chancellor/invoke-public-commit-governance-gate.ps1` 扩展 `Get-CanonicalPanelCommandState`，新增两组固定子项校验。
5. 在 `.codex/chancellor/test-public-commit-governance-gate.ps1` 增补回归测试，覆盖入口验收文档与人工验板清单的两类子项漂移拦截。
6. 更新 `docs/30-方案/09-V4-本机生产切换最小闭环方案.md`，把本轮治理沉淀回当前现状。

## 结果

- `丞相帮助` 的 `当前用法` 与 `面板命令` 已从笼统标题收敛为固定子项模板。
- 入口验收文档、人工验板清单、公开提交治理门禁三处继续共享同一套公开口径。
- 若未来有人把默认入口、维护层说明或公开边界写歪，提交前会被硬门禁拦截。

## 下一步

1. 评估是否把 `丞相状态` 与 `丞相检查` 的人话输出继续固化成更细的固定槽位。
2. 保持桥接切换实话口径：当前是 `bridge-ready`，不是“全量生产母体重构已完成”。
