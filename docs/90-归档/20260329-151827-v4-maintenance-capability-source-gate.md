# V4 维护层补充入口真源化记录

时间：2026-03-29 15:18:27
任务：第三十五刀（维护层补充入口真源化）

## 动作

1. 复盘公开提交治理门禁剩余硬编码，识别出 `README.md` 与 `docs/README.md` 的维护层补充入口仍未直接读取维护层总入口真源。
2. 扩展 `.codex/chancellor/invoke-public-commit-governance-gate.ps1`，新增从 `docs/40-执行/13-维护层总入口.md` 的“当前维护层能力”区块提取维护层补充入口集合的逻辑。
3. 保持现行总览“阅读顺序建议”对维护层主线顺序的既有职责不变，只把 `README.md` 与 `docs/README.md` 的维护层补充入口集合校验下沉到维护层总入口真源，避免把不同语义的顺序体系硬绑在一起。
4. 扩展 `.codex/chancellor/test-public-commit-governance-gate.ps1`，新增“公开页缺少归档规则入口即失败”“维护层总入口删掉归档规则来源即失败”两条回归，并补齐 `10`、`11` 同时受执行区真源与维护层真源约束后的老用例前置。
5. 同步更新 `.codex/chancellor/README.md`、`docs/40-执行/10-本地安全提交流程.md`、`docs/30-方案/08-V4-治理审计候选规范.md` 与 `docs/40-执行/13-维护层总入口.md`，明确新的真源关系。

## 结果

- 当前自动门禁已把 `README.md` 与 `docs/README.md` 的维护层补充入口集合改为由 `docs/40-执行/13-维护层总入口.md` 驱动。
- 删除公开页中的 `docs/90-归档/01-执行区证据稿归档规则.md` 入口时，门禁会自动失败。
- 删除维护层总入口中的对应来源时，门禁也会因出现未受控维护层补充入口而自动失败。
- 现行总览阅读顺序与维护层主线顺序门禁保持原职责，没有被混入新的不同语义集合。

## 理由

- `README.md` 与 `docs/README.md` 承担的是对外总入口职责，维护层补充入口是否完整，最该由维护层总入口自身提供真源，而不是继续散落在脚本中维护。
- 直接改成“存在性集合真源”而不强行统一顺序，既能提升自动约束强度，又能避免把 `90-归档`、治理审计等不同语义项硬塞进现行总览阅读顺序。

## 下一步

1. 暂存本轮公开安全改动并执行暂存区门禁。
2. 完成 `commit`、`pull --rebase origin main` 与 `push origin main`。
3. 下一刀可继续寻找仍由脚本硬编码、但本质应由专门总入口文档供给的公开口径集合。
