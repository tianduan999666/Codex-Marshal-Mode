# V4 维护层主线真源化记录

时间：2026-03-29 15:40:13
任务：第三十七刀（维护层主线真源化）

## 动作

1. 复盘公开提交治理门禁剩余硬编码，确认维护层主线顺序仍通过现行总览“阅读顺序建议”切片推导，缺少维护层自身的专用真源。
2. 在 `docs/40-执行/13-维护层总入口.md` 新增“维护层主线真源”区块，显式列出 `13` 到 `21` 的维护层主线文档顺序。
3. 扩展 `.codex/chancellor/invoke-public-commit-governance-gate.ps1`，把维护层主线顺序改为读取 `13-维护层总入口` 的主线真源区块，而不再由现行总览切片间接提供。
4. 保持现行总览“阅读顺序建议”继续承担公开阅读顺序展示职责，并让门禁反向校验它是否与新的维护层主线真源一致。
5. 扩展 `.codex/chancellor/test-public-commit-governance-gate.ps1`，新增“交换维护层主线真源中的 `19` / `20` 顺序时必须失败”的回归测试。
6. 同步更新 `.codex/chancellor/README.md`、`docs/40-执行/10-本地安全提交流程.md`、`docs/30-方案/08-V4-治理审计候选规范.md` 与 `docs/40-执行/13-维护层总入口.md`，明确新的真源关系。

## 结果

- 当前自动门禁已把维护层主线顺序的真源收回 `docs/40-执行/13-维护层总入口.md`。
- 现行总览“阅读顺序建议”不再兼任维护层主线真源，而是被门禁校验为需与真源保持一致的公开阅读口径。
- 当维护层主线真源中的 `19` / `20` 被调换时，门禁会自动失败，证明维护层顺序已真正改由维护层自身供源。

## 理由

- 维护层主线是维护层自己的长期秩序，最稳的做法是由维护层总入口自己声明，而不是继续借用导航总览的切片结果。
- 这样做短期多一处真源区块，但长期更清楚：导航负责展示，维护层入口负责维护层秩序，职责更干净。

## 下一步

1. 暂存本轮公开安全改动并执行暂存区门禁。
2. 完成 `commit`、`pull --rebase origin main` 与 `push origin main`。
3. 下一刀可继续寻找仍借道其他文档切片、但本质应由专用总入口自己供源的主线或集合约束。
