# V4 复杂并存 decision-log 骨架槽位门禁落地记录

时间：2026-03-30 07:09:00
任务：第一百一十一刀（复杂并存汇报骨架模板 decision-log.md 推荐骨架固定槽位硬门禁）

## 动作

1. 在 `docs/40-执行/20-复杂并存汇报骨架模板.md` 新增 ``decision-log.md` 推荐骨架固定槽位` 区块。
2. 在 `.codex/chancellor/invoke-public-commit-governance-gate.ps1` 新增固定槽位校验，并将其挂入预计算链。
3. 在 `.codex/chancellor/test-public-commit-governance-gate.ps1` 增补回归测试，覆盖 decision-log.md 推荐骨架固定槽位漂移拦截。
4. 更新 `docs/30-方案/09-V4-本机生产切换最小闭环方案.md`，把本轮治理沉淀回当前现状。

## 理由

- ``decision-log.md` 推荐骨架` 是复杂并存裁决留痕的主契约；如果继续停留在自由文本层，最容易把决策、证据、未选状态与影响写散。
- 先把这层收敛为固定槽位，再下沉到同一公开提交治理门禁，可以把复杂并存裁决留痕契约长期钉死。

## 结果

- ``decision-log.md` 推荐骨架` 已从散列骨架收敛为固定槽位模板。
- `20-复杂并存汇报骨架模板.md` 与公开提交治理门禁已共用同一套 decision-log 留痕真源。
- `决策 → 主阻塞 → 原因 → 证据 → 未选状态 → 裁决依据 → 治理提示 → 影响` 已可被自动门禁与回归脚本稳定拦截漂移。

## 下一步

1. `20-复杂并存汇报骨架模板.md` 的核心高频公开口径已基本硬门禁化，可继续盘点剩余可模板化块。
2. 如继续推进，优先考虑把 `docs/40-执行/03-面板入口验收.md`、`codex-home-export/panel-acceptance-checklist.md` 等既有基线漂移最密集入口转为同样的固定槽位收敛。
