# V4 重启导读顺序真源门禁记录

时间：2026-03-29 13:40:49
任务：v4-trial-029-restart-guide-order-source-gate

## 动作

1. 识别长期维护瓶颈：启动阶段顺序虽然已进入硬门禁，但仍由脚本硬编码维护，未来容易与重启导读区块再次分叉。
2. 扩展 `.codex/chancellor/invoke-public-commit-governance-gate.ps1`，把启动阶段关键入口顺序改为直接读取 `docs/00-导航/01-V4-重启导读.md` 的“先看什么”区块，再自动截到 `docs/30-方案/03-V4-MVP边界清单.md` 为止。
3. 扩展 `.codex/chancellor/test-public-commit-governance-gate.ps1`，新增“改动重启导读顺序但未同步两个公开首页时必须失败”的场景。
4. 同步更新 `.codex/chancellor/README.md`、`docs/40-执行/10-本地安全提交流程.md`、`docs/30-方案/08-V4-治理审计候选规范.md`，明确启动阶段顺序真源已改为重启导读区块。

## 结果

- 当前自动门禁已把“启动阶段顺序”从脚本硬编码提升为“重启导读 section 真源”。
- 当前仓现状通过新门禁。
- 人为调换重启导读中的 `重启ADR / 必需资产清单` 顺序而不同步两个公开首页时，门禁会自动失败。

## 理由

- 真源若仍在脚本里，文档一改就要同时手改脚本，长期必然漂移。
- 直接以现行文档区块为真源，短期实现更难，但长期最稳、最省心，也最符合 V8 的复利要求。

## 下一步

1. 暂存本轮公开安全改动并执行提交。
2. 完成 `pull --rebase origin main` 与 `push origin main`，确认 `pre-push` 自动门禁通过。
3. 回写任务包状态并清空活动指针。
