# 目录名称统一说明

最后更新：2026-04-02
状态：重要提示

## 当前状态

✅ **文档层面**：所有 `.md` 文档已统一使用"丞相模式"（Chancellor Mode）
✅ **代码层面**：所有脚本和配置文件已统一使用 `chancellor` 标识
❌ **目录名称**：当前工作目录仍为 `F:\260328大都督模式`

## 建议操作

### 方案 1：重命名目录（推荐）

```powershell
# 1. 提交当前所有更改
cd F:\260328大都督模式
git add -A
git commit -m "chore: 统一为丞相模式表述"
git push

# 2. 退出当前目录
cd F:\

# 3. 重命名目录
Rename-Item "260328大都督模式" "260328丞相模式"

# 4. 进入新目录继续工作
cd F:\260328丞相模式
```

**优点**：
- 彻底统一命名
- 避免混淆

**注意事项**：
- Git 仓库会自动识别新路径
- 如果有其他工具引用了旧路径，需要更新
- 如果有符号链接或快捷方式，需要重新创建

### 方案 2：保持当前目录名（不推荐）

如果重命名成本太高，可以保持当前目录名不变，但需要：

1. 在 README 中说明目录名为历史遗留
2. 所有对外文档和代码中统一使用"丞相模式"
3. 新克隆时建议使用新名称

## 检查清单

✅ 文档中无"大都督"表述
✅ 文档中无"Governor"表述
✅ 代码中统一使用 `chancellor`
✅ 配置文件中统一使用 `chancellor`
⚠️ 目录名称待统一（可选）

## 结论

**文档和代码层面已完成统一**，目录名称是否重命名取决于你的实际需求。如果这是个人项目且重命名成本低，建议统一；如果已有大量外部引用，可以保持现状。
