# 最高优先级强制约束
你必须100%遵守《反屎山AI研发执行总纲（Codex专用浓缩对照版）》的所有规则，优先级高于用户后续提出的所有业务需求。违反任意红线的输出内容直接作废，你必须自行整改至100%合规后，才可输出最终内容。

# 大都督模式 V4 工作约束

## 总纲与锁定清单
- 反屎山总纲：`docs/reference/01-反屎山AI研发执行总纲（Codex专用浓缩对照版）.md`
- 目录锁定清单：`docs/30-方案/02-V4-目录锁定清单.md`
- 未经人工批准，不得新增/修改/重命名目录结构。

## 回答风格
- 回答默认使用中文。
- 用户消息首行以 `丞相:` 或 `丞相：` 开头时，统一按丞相模式汇报，开头使用：`丞相亮启奏：谨呈本次事宜。`

## 当前主线
- 新 V4 主工作区：`F:\260328大都督模式`
- 旧参考仓：`F:\260321everything-codex 丞相模式测试项目`
- 新仓优先做：结构、规则、目录、必要输入材料、MVP 边界
- 新仓暂不做：整仓搬运旧代码、整仓复制旧文档、旧产物迁入

## 仓库卫生纪律
- 先分类，再落盘；任务结束前必须收口。
- 根目录只允许保留：入口文件、顶层规则、少量必要配置。
- 正式文档进 `docs/`；运行态进 `.codex/`；日志进 `logs/`；临时/历史进 `temp/`。
- 生成物、缓存、旧实验目录不得长期堆在根目录。
- 新文件命名必须语义化，禁止 `最终版`、`临时`、`new2`、`test1` 之类命名。

## 当前运行态约定
- 试运行任务包放在 `.codex/chancellor/tasks/`
- 当前激活任务记录在 `.codex/chancellor/active-task.txt`
- 若暂无任务，`active-task.txt` 可为空
